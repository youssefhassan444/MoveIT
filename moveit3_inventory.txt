=== FILE: lib/main.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- Function: main()
  - Signature: void main() async
  - Parameters: None
  - Return type: void (Future<void> inside runZonedGuarded)
  - Local variables: None (except inside closures)
  - Description: Entry point of the application. Initializes WidgetsFlutterBinding, Firebase, Crashlytics, and runs the ProviderScope wrapped MoveItApp.

#### 2. CLASS DECLARATIONS
- Class: MoveItApp
  - Signature: class MoveItApp extends ConsumerWidget
  - Fields: None
  - Methods:
    - Constructor: const MoveItApp({super.key})
    - Method: build(BuildContext context, WidgetRef ref)
      - Parameters: context (BuildContext), ref (WidgetRef)
      - Return type: Widget
      - Local variables:
        - router (GoRouter): Obtained via ref.watch(routerProvider).

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: ProviderScope
Constructor arguments:
  key: null
  child: MoveItApp()
  overrides: []
  observers: null
Layout context:
  Parent: Root of the application (passed to runApp)
  This is the top-level widget that enables Riverpod state management.
// WHY: Required for Riverpod dependency injection and state management across the app.

WIDGET INSTANCE:
Type: MoveItApp
Constructor arguments:
  key: null (passed as super.key)
Layout context:
  Parent: ProviderScope
// WHY: The root component of the application logic.

WIDGET INSTANCE:
Type: MaterialApp.router
Constructor arguments:
  key: null
  title: 'MoveIt'
  debugShowCheckedModeBanner: false
  theme: AppTheme.light (resolves to ThemeData, see lib/core/theme/app_theme.dart)
  darkTheme: AppTheme.dark (resolves to ThemeData, see lib/core/theme/app_theme.dart)
  themeMode: ThemeMode.system
  routerConfig: router (GoRouter instance from routerProvider)
Layout context:
  Parent: MoveItApp.build
// WHY: Standard Flutter entry point for Material applications using the Router API (GoRouter). ThemeMode.system ensures the app respects user's OS preference.

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in main():
  - `try-catch` block: Wraps Firebase initialization.
  - `if (Firebase.apps.isEmpty)`: Checks if Firebase is already initialized to prevent errors on hot restart.
- State Changes:
  - `ref.watch(routerProvider)`: Subscribes to the router provider. If the router configuration changes, the entire MaterialApp.router rebuilds.
- Navigation:
  - Managed by `routerConfig: router`. Navigation is handled via GoRouter (see `lib/core/router/app_router.dart`).

#### 5. LIFECYCLE AND CLEANUP
- `runZonedGuarded`: Used to catch and record asynchronous errors that are not caught by the Flutter framework.
- `FlutterError.onError`: Captures synchronous errors in the Flutter framework.
- `PlatformDispatcher.instance.onError`: Captures errors in the platform dispatcher.
// WHY: Comprehensive error tracking via Firebase Crashlytics for production stability.

CONTINUE TO NEXT FILE
=== FILE: lib/core/router/app_router.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- Constant: routerProvider
  - Type: Provider<GoRouter>
  - Initial value: Provider<GoRouter>((ref) { ... returns GoRouter ... })
  - Description: Provides the GoRouter instance used for application navigation. Defines all routes and shells.

#### 2. CLASS DECLARATIONS
- None (Only the provider is declared in this file).

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(This file defines the routing logic, but uses widgets in the builders.)

WIDGET INSTANCE:
Type: GoRouter
Constructor arguments:
  initialLocation: '/auth'
  routes: [GoRoute(...), ShellRoute(...), ...]
Layout context:
  Managed by routerProvider.
// WHY: Centralized navigation management. '/auth' is the initial location to ensure the AuthWrapper checks the user session first.

WIDGET INSTANCE (Inside /auth builder):
Type: AuthWrapper
Constructor arguments:
  key: null
Layout context:
  Parent: GoRoute(/auth)
// WHY: Decides whether to show login or home screens based on auth state.

WIDGET INSTANCE (Inside /login builder):
Type: LoginScreen
Constructor arguments:
  key: null
Layout context:
  Parent: GoRoute(/login)

WIDGET INSTANCE (Inside /signup builder):
Type: SignupScreen
Constructor arguments:
  key: null
Layout context:
  Parent: GoRoute(/signup)

WIDGET INSTANCE (Inside Customer shell builder):
Type: CustomerShell
Constructor arguments:
  key: null
  child: <Sub-route widget>
Layout context:
  Parent: ShellRoute (Customer)
// WHY: Provides a persistent navigation bar/drawer for customer-specific routes.

WIDGET INSTANCE (Inside Driver shell builder):
Type: DriverShell
Constructor arguments:
  key: null
  child: <Sub-route widget>
Layout context:
  Parent: ShellRoute (Driver)
// WHY: Provides a persistent navigation bar/drawer for driver-specific routes.

WIDGET INSTANCE (Inside /customer/history builder):
Type: Scaffold
Constructor arguments:
  body: Center(child: Text('History — coming soon'))
Layout context:
  Parent: GoRoute(/customer/history)
// WHY: Placeholder for future history feature.

WIDGET INSTANCE (Inside /driver/history builder):
Type: Scaffold
Constructor arguments:
  body: Center(child: Text('History — coming soon'))
Layout context:
  Parent: GoRoute(/driver/history)
// WHY: Placeholder for future history feature.

#### 4. CONTROL FLOW AND STATE MAPPING
- Routing Logic:
  - Initial Location: /auth.
  - Shells: Two distinct ShellRoutes for Customer and Driver to maintain role-based navigation states.
- Navigation:
  - CustomerJobDetailScreen: Extracts jobId from state.pathParameters['id']!.
- State Changes:
  - The outerProvider itself is static but can be made dynamic if redirection logic based on auth state were added here (though it's currently handled in AuthWrapper).

#### 5. LIFECYCLE AND CLEANUP
- The GoRouter instance is created once by the provider and persists.

CONTINUE TO NEXT FILE
=== FILE: lib/core/theme/app_theme.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: AppTheme
  - Signature: class AppTheme
  - Fields:
    - static const Color brandNavy: Color(0xFF0F1F91)
    - static const Color brandSkyBlue: Color(0xFF1265B3)
    - static const Color brandOrange: Color(0xFFF79529)
    - static const Color brandError: Color(0xFFD32F2F)
    - static const Color brandSuccess: Color(0xFF2E7D32)
    - static const Color nearBlack: Color(0xFF1A1A2E)
  - Methods:
    - static ThemeData get light: Calls _build(Brightness.light)
    - static ThemeData get dark: Calls _build(Brightness.dark)
    - static ThemeData _build(Brightness brightness):
      - Parameters: brightness (Brightness)
      - Return type: ThemeData
      - Local variables:
        - base (ThemeData): Result of ThemeData.light() or ThemeData.dark()
      - Description: Constructs the theme based on the provided brightness, using Google Fonts (Cairo) and custom brand colors.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(This file defines the theme data, not a widget tree per se, but it affects the entire UI.)

#### 4. CONTROL FLOW AND STATE MAPPING
- Logic in _build:
  - rightness == Brightness.light ? ThemeData.light() : ThemeData.dark(): Sets the base theme.
  - surface: rightness == Brightness.light ? Colors.white : nearBlack.
  - scaffoldBackgroundColor: rightness == Brightness.light ? Colors.white : nearBlack.
  - illColor: rightness == Brightness.light ? Colors.grey[100] : Colors.grey[900].
// WHY: Ensures high contrast and readability in both light and dark modes. Cairo font provides a modern, clean look suitable for the brand identity.

#### 5. LIFECYCLE AND CLEANUP
- Static class, no lifecycle management needed.

CONTINUE TO NEXT FILE
=== FILE: lib/firebase_options.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- Class: DefaultFirebaseOptions
  - Description: Automatically generated by FlutterFire CLI. Contains Firebase configuration for different platforms.
  - Constants:
    - web (FirebaseOptions): API Key 'AIza...Aqc', App ID '1:85...257', Project ID 'moveit-v2-e8079'.
    - android (FirebaseOptions): API Key 'AIza...flo', App ID '1:85...257', Project ID 'moveit-v2-e8079'.
    - ios (FirebaseOptions): API Key 'AIza...u9E', App ID '1:85...257', Project ID 'moveit-v2-e8079'.

#### 2. CLASS DECLARATIONS
- Class: DefaultFirebaseOptions
  - Methods:
    - static FirebaseOptions get currentPlatform: Returns the appropriate FirebaseOptions based on the current platform (Web, Android, iOS). Throws UnsupportedError for macOS, Windows, and Linux.

// WHY: Generated file required for Firebase initialization. Centralizes platform-specific API keys and IDs.

CONTINUE TO NEXT FILE
=== FILE: lib/features/auth/auth_wrapper.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: AuthWrapper
  - Signature: class AuthWrapper extends ConsumerWidget
  - Fields: None
  - Methods:
    - Constructor: const AuthWrapper({super.key})
    - Method: build(BuildContext context, WidgetRef ref)
      - Parameters: context (BuildContext), ref (WidgetRef)
      - Return type: Widget
      - Local variables:
        - authState (AsyncValue<User?>): Watches authStateChangesProvider.
        - userDoc (AsyncValue<UserModel?>): Watches currentUserDocProvider.

- Class: _LoadingScreen
  - Signature: class _LoadingScreen extends StatelessWidget
  - Fields: None
  - Methods:
    - Constructor: const _LoadingScreen()
    - Method: build(BuildContext context)

- Class: _Redirector
  - Signature: class _Redirector extends StatefulWidget
  - Fields:
    - final String target
  - Methods:
    - Constructor: const _Redirector({required this.target})
    - Method: createState() returns _RedirectorState

- Class: _RedirectorState
  - Signature: class _RedirectorState extends State<_Redirector>
  - Methods:
    - Method: initState()
    - Method: build(BuildContext context)

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE (AuthWrapper.build - loading branch):
Type: _LoadingScreen
Constructor arguments:
  key: null
Layout context:
  Parent: AuthWrapper.build (inside authState.when loading or userDoc.when loading)
// WHY: Provides a consistent splash-like loading experience while checking auth state or fetching user profile.

WIDGET INSTANCE (AuthWrapper.build - error branch):
Type: ErrorStateWidget
Constructor arguments:
  message: "Authentication error. Please try again." (or similar profile error message)
  onRetry: () => ref.invalidate(authStateChangesProvider) (or currentUserDocProvider)
Layout context:
  Parent: AuthWrapper.build (inside authState.when error or userDoc.when error)
// WHY: Graceful error handling for network or service failures.

WIDGET INSTANCE (AuthWrapper.build - authenticated/unauthenticated data branch):
Type: _Redirector
Constructor arguments:
  target: '/login' (if user == null) or '/customer' (if model.role == 'customer') or '/driver' (otherwise)
Layout context:
  Parent: AuthWrapper.build (inside authState.when data)
// WHY: Silently redirects the user to the appropriate screen based on their authentication status and role.

WIDGET INSTANCE (_LoadingScreen.build):
Type: Scaffold
Constructor arguments:
  backgroundColor: Color(0xFF0F1F91) (AppTheme.brandNavy)
  body: Center(child: CircularProgressIndicator(...))
Layout context:
  Parent: _LoadingScreen.build
// WHY: Branding-consistent full-screen loading.

WIDGET INSTANCE (_LoadingScreen.build -> body):
Type: Center
Constructor arguments:
  child: CircularProgressIndicator(...)
Layout context:
  Parent: Scaffold

WIDGET INSTANCE (_LoadingScreen.build -> body -> child):
Type: CircularProgressIndicator
Constructor arguments:
  valueColor: AlwaysStoppedAnimation<Color>(Color(0xFF1265B3) (AppTheme.brandSkyBlue))
Layout context:
  Parent: Center

WIDGET INSTANCE (_Redirector.build):
Type: _LoadingScreen
Constructor arguments:
  key: null
Layout context:
  Parent: _Redirector.build
// WHY: Displays loading screen while the redirect occurs.

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in AuthWrapper.build:
  - uthState.when: 
    - data (user):
      - if (user == null): Redirects to /login.
      - else: 
        - userDoc.when:
          - data (model):
            - if (model == null): Shows ErrorStateWidget.
            - if (model.role == 'customer'): Redirects to /customer.
            - else: Redirects to /driver.
- Navigation:
  - context.go(widget.target): Called inside WidgetsBinding.instance.addPostFrameCallback in _RedirectorState.initState.
- State Changes:
  - ef.watch(authStateChangesProvider): Rebuilds on auth state changes.
  - ef.watch(currentUserDocProvider): Rebuilds when the user document is fetched/updated.

#### 5. LIFECYCLE AND CLEANUP
- _RedirectorState.initState:
  - Adds a post-frame callback to execute the navigation after the widget is rendered.
// WHY: Navigation should not happen during the build phase; post-frame callback ensures the context is ready.

CONTINUE TO NEXT FILE
=== FILE: lib/features/auth/login_screen.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: LoginScreen
  - Signature: class LoginScreen extends HookConsumerWidget
  - Fields: None
  - Methods:
    - Constructor: const LoginScreen({super.key})
    - Method: build(BuildContext context, WidgetRef ref)
      - Parameters: context (BuildContext), ref (WidgetRef)
      - Return type: Widget
      - Local variables:
        - emailCtrl (TextEditingController): Managed by useTextEditingController().
        - passCtrl (TextEditingController): Managed by useTextEditingController().
        - loading (ValueNotifier<bool>): Managed by useState(false).
        - formKey (GlobalKey<FormState>): Managed by useMemoized().
        - login (Future<void> function): Handles sign-in logic.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  key: null
  backgroundColor: Colors.white (Color(0xFFFFFFFF))
  body: SafeArea(...)
Layout context:
  Parent: LoginScreen.build
// WHY: Provides a clean, white background for the login form, adhering to the Material design guidelines.

WIDGET INSTANCE:
Type: SafeArea
Constructor arguments:
  child: SingleChildScrollView(...)
Layout context:
  Parent: Scaffold

WIDGET INSTANCE:
Type: SingleChildScrollView
Constructor arguments:
  child: Padding(...)
Layout context:
  Parent: SafeArea
// WHY: Ensures the form is scrollable on smaller screens or when the keyboard is visible.

WIDGET INSTANCE:
Type: Padding
Constructor arguments:
  padding: EdgeInsets.only(left: 24.0, top: 24.0, right: 24.0, bottom: 24.0) (EdgeInsets.all(24.0))
  child: Form(...)
Layout context:
  Parent: SingleChildScrollView

WIDGET INSTANCE:
Type: Form
Constructor arguments:
  key: formKey
  child: Column(...)
Layout context:
  Parent: Padding

WIDGET INSTANCE:
Type: Column
Constructor arguments:
  mainAxisAlignment: MainAxisAlignment.center
  crossAxisAlignment: CrossAxisAlignment.center (default)
  children: [SizedBox, Image, SizedBox, Text, SizedBox, Text, SizedBox, TextFormField, SizedBox, TextFormField, SizedBox, SizedBox, SizedBox, Text, SizedBox, SizedBox, SizedBox, Padding, SizedBox]
Layout context:
  Parent: Form

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 20.0
Layout context:
  Parent: Column (1st child)

WIDGET INSTANCE:
Type: Image.asset
Constructor arguments:
  name: "assets/moveit_master/moveit.png"
  height: 120.0
Layout context:
  Parent: Column (2nd child)
// WHY: Displays the brand logo prominently.

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 30.0
Layout context:
  Parent: Column (3rd child)

WIDGET INSTANCE:
Type: Text
Constructor arguments:
  data: "You already have an account"
  style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold, color: Colors.black)
  textAlign: TextAlign.center
Layout context:
  Parent: Column (4th child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 10.0
Layout context:
  Parent: Column (5th child)

WIDGET INSTANCE:
Type: Text
Constructor arguments:
  data: "Enter your email to login for this app"
  style: TextStyle(fontSize: 15, fontWeight: FontWeight.bold, color: Colors.black54)
Layout context:
  Parent: Column (6th child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 40.0
Layout context:
  Parent: Column (7th child)

WIDGET INSTANCE:
Type: TextFormField
Constructor arguments:
  controller: emailCtrl
  keyboardType: TextInputType.emailAddress
  decoration: InputDecoration(
    hintText: "email@domain.com",
    prefixIcon: Icon(Icons.email),
    border: OutlineInputBorder(borderRadius: BorderRadius.circular(10)),
    filled: true,
    fillColor: Colors.grey[50] (Color(0xFFFAFAFA)),
  )
  validator: (v) => (v == null || !v.contains('@')) ? 'Enter a valid email' : null
Layout context:
  Parent: Column (8th child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 16.0
Layout context:
  Parent: Column (9th child)

WIDGET INSTANCE:
Type: TextFormField
Constructor arguments:
  controller: passCtrl
  keyboardType: TextInputType.visiblePassword
  obscureText: true
  decoration: InputDecoration(
    labelText: "password",
    hintText: "Enter your password",
    prefixIcon: Icon(Icons.lock),
    border: OutlineInputBorder(borderRadius: BorderRadius.circular(10)),
    filled: true,
    fillColor: Colors.grey[50] (Color(0xFFFAFAFA)),
  )
  validator: (v) => (v == null || v.length < 6) ? 'Password must be 6+ characters' : null
Layout context:
  Parent: Column (10th child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 40.0
Layout context:
  Parent: Column (11th child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 56.0
  width: double.infinity
  child: ElevatedButton(...)
Layout context:
  Parent: Column (12th child)
// WHY: Defines the layout constraints for the login button.

WIDGET INSTANCE:
Type: ElevatedButton
Constructor arguments:
  onPressed: loading.value ? null : login
  style: ElevatedButton.styleFrom(
    backgroundColor: Color(0xFFF79529) (AppTheme.brandOrange),
    foregroundColor: Colors.white,
    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),
    elevation: 2.0,
  )
  child: loading.value ? CircularProgressIndicator(...) : Text("LOGIN", ...)
Layout context:
  Parent: SizedBox
// WHY: Prominent CTA for logging in. Orange color differentiates it from the primary brand navy.

WIDGET INSTANCE (Inside ElevatedButton):
Type: CircularProgressIndicator
Constructor arguments:
  color: Colors.white
  strokeWidth: 2.0
Layout context:
  Parent: SizedBox (24x24) inside ElevatedButton
// WHY: Shows activity when the user clicks the button.

WIDGET INSTANCE (Inside ElevatedButton):
Type: Text
Constructor arguments:
  data: "LOGIN"
  style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)
Layout context:
  Parent: ElevatedButton

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 20.0
Layout context:
  Parent: Column (13th child)

WIDGET INSTANCE:
Type: Text
Constructor arguments:
  data: "or"
  style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold, color: Colors.black45)
Layout context:
  Parent: Column (14th child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 20.0
Layout context:
  Parent: Column (15th child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 56.0
  width: double.infinity
  child: ElevatedButton(...)
Layout context:
  Parent: Column (16th child)

WIDGET INSTANCE:
Type: ElevatedButton
Constructor arguments:
  onPressed: () => context.push('/signup')
  style: ElevatedButton.styleFrom(
    backgroundColor: Color(0xFF0F1F91) (AppTheme.brandNavy),
    foregroundColor: Colors.white,
    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),
    elevation: 2.0,
  )
  child: Text("SIGN UP", ...)
Layout context:
  Parent: SizedBox
// WHY: Secondary CTA for new users. Brand navy color establishes brand identity.

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 30.0
Layout context:
  Parent: Column (17th child)

WIDGET INSTANCE:
Type: Padding
Constructor arguments:
  padding: EdgeInsets.only(left: 20.0, top: 0.0, right: 20.0, bottom: 0.0)
  child: Text("By clicking continue, you agree to our terms...", ...)
Layout context:
  Parent: Column (18th child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 20.0
Layout context:
  Parent: Column (19th child)

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in login():
  - if (!formKey.currentState!.validate()): Checks form validity. Triggers HapticFeedback.mediumImpact() if invalid.
  - loading.value = true: Sets loading state.
  - inal result = await ref.read(authServiceProvider).signIn(...): Calls auth service.
  - loading.value = false: Resets loading state.
  - esult.when:
    - success: (_) => context.go('/auth'): Navigates back to AuthWrapper on success.
    - ailure: (error): Triggers HapticFeedback.heavyImpact() and shows a snackbar.
- Navigation:
  - context.go('/auth'): On successful login.
  - context.push('/signup'): On clicking SIGN UP.
- State Changes:
  - loading: Reactive state variable that controls button interactivity and child widget (Text vs Spinner).
  - ormKey: Used to validate the form fields.

#### 5. LIFECYCLE AND CLEANUP
- useTextEditingController(): Automatically disposes controllers when the widget is unmounted.
- useState(false): Manages local loading state.

CONTINUE TO NEXT FILE
=== FILE: lib/features/auth/signup_screen.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: SignupScreen
  - Signature: class SignupScreen extends HookConsumerWidget
  - Fields: None
  - Methods:
    - Constructor: const SignupScreen({super.key})
    - Method: build(BuildContext context, WidgetRef ref)
      - Parameters: context (BuildContext), ref (WidgetRef)
      - Return type: Widget
      - Local variables:
        - nameCtrl (TextEditingController): Managed by useTextEditingController().
        - emailCtrl (TextEditingController): Managed by useTextEditingController().
        - passCtrl (TextEditingController): Managed by useTextEditingController().
        - confirmCtrl (TextEditingController): Managed by useTextEditingController().
        - role (ValueNotifier<String>): Managed by useState('customer').
        - vehicleType (ValueNotifier<String>): Managed by useState('motorcycle').
        - loading (ValueNotifier<bool>): Managed by useState(false).
        - formKey (GlobalKey<FormState>): Managed by useMemoized().
        - signup (Future<void> function): Handles registration logic.

- Class: _RoleToggle
  - Signature: class _RoleToggle extends StatelessWidget
  - Fields:
    - final String selected
    - final ValueChanged<String> onChanged
  - Methods:
    - Constructor: const _RoleToggle({required this.selected, required this.onChanged})
    - Method: build(BuildContext context)

- Class: _Tab
  - Signature: class _Tab extends StatelessWidget
  - Fields:
    - final String label, value, selected
    - final ValueChanged<String> onChanged
  - Methods:
    - Constructor: const _Tab({required this.label, required this.value, required this.selected, required this.onChanged})
    - Method: build(BuildContext context)

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  key: null
  backgroundColor: Colors.white
  body: SafeArea(...)
Layout context:
  Parent: SignupScreen.build

WIDGET INSTANCE:
Type: SafeArea
Constructor arguments:
  child: Center(...)
Layout context:
  Parent: Scaffold

WIDGET INSTANCE:
Type: Center
Constructor arguments:
  child: SingleChildScrollView(...)
Layout context:
  Parent: SafeArea

WIDGET INSTANCE:
Type: SingleChildScrollView
Constructor arguments:
  padding: EdgeInsets.only(left: 24.0, top: 24.0, right: 24.0, bottom: 24.0)
  child: Form(...)
Layout context:
  Parent: Center

WIDGET INSTANCE:
Type: Form
Constructor arguments:
  key: formKey
  child: Column(...)
Layout context:
  Parent: SingleChildScrollView

WIDGET INSTANCE:
Type: Column
Constructor arguments:
  children: [Image, SizedBox, Text, SizedBox, _RoleToggle, SizedBox, TextFormField, SizedBox, TextFormField, SizedBox, TextFormField, SizedBox, TextFormField, AnimatedSize, SizedBox, SizedBox, TextButton]
Layout context:
  Parent: Form

WIDGET INSTANCE:
Type: Image.asset
Constructor arguments:
  name: "assets/moveit_master/moveit.png"
  height: 100.0
Layout context:
  Parent: Column (1st child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 20.0
Layout context:
  Parent: Column (2nd child)

WIDGET INSTANCE:
Type: Text
Constructor arguments:
  data: "Sign Up"
  style: TextStyle(fontSize: 30, fontWeight: FontWeight.bold, color: Colors.amber (Color(0xFFFFC107)))
Layout context:
  Parent: Column (3rd child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 30.0
Layout context:
  Parent: Column (4th child)

WIDGET INSTANCE:
Type: _RoleToggle
Constructor arguments:
  selected: role.value
  onChanged: (v) => role.value = v
Layout context:
  Parent: Column (5th child)
// WHY: Allows the user to select between 'Customer' and 'Driver' roles, which dynamically changes the registration fields.

WIDGET INSTANCE (_RoleToggle.build):
Type: Container
Constructor arguments:
  height: 48.0
  decoration: BoxDecoration(color: Colors.grey[100], borderRadius: BorderRadius.circular(12))
  child: Row(...)
Layout context:
  Parent: _RoleToggle.build

WIDGET INSTANCE (_RoleToggle.build -> Row):
Type: Row
Constructor arguments:
  children: [_Tab(Customer), _Tab(Driver)]
Layout context:
  Parent: Container

WIDGET INSTANCE (_Tab.build):
Type: Expanded
Constructor arguments:
  child: GestureDetector(...)
Layout context:
  Parent: Row

WIDGET INSTANCE (_Tab.build -> GestureDetector):
Type: GestureDetector
Constructor arguments:
  onTap: () { HapticFeedback.lightImpact(); onChanged(value); }
  child: AnimatedContainer(...)
Layout context:
  Parent: Expanded

WIDGET INSTANCE (_Tab.build -> GestureDetector -> AnimatedContainer):
Type: AnimatedContainer
Constructor arguments:
  duration: Duration(milliseconds: 200)
  alignment: Alignment.center
  decoration: BoxDecoration(
    color: active ? AppTheme.brandSkyBlue : Colors.transparent,
    borderRadius: BorderRadius.circular(12),
  )
  child: Text(label, style: TextStyle(color: active ? Colors.white : AppTheme.brandNavy, fontWeight: FontWeight.bold))
Layout context:
  Parent: GestureDetector

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 24.0
Layout context:
  Parent: Column (6th child)

WIDGET INSTANCE:
Type: TextFormField
Constructor arguments:
  controller: nameCtrl
  textCapitalization: TextCapitalization.words
  decoration: InputDecoration(
    labelText: "Name",
    border: OutlineInputBorder(borderRadius: BorderRadius.circular(10)),
    filled: true,
    fillColor: Colors.grey[50],
  )
  validator: (v) => (v == null || v.trim().isEmpty) ? 'Enter your name' : null
Layout context:
  Parent: Column (7th child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 16.0
Layout context:
  Parent: Column (8th child)

WIDGET INSTANCE:
Type: TextFormField (Email)
Constructor arguments:
  controller: emailCtrl
  keyboardType: TextInputType.emailAddress
  decoration: InputDecoration(labelText: "Email", ...)
  validator: (v) => (v == null || !v.contains('@')) ? 'Enter a valid email' : null
Layout context:
  Parent: Column (9th child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 16.0
Layout context:
  Parent: Column (10th child)

WIDGET INSTANCE:
Type: TextFormField (Password)
Constructor arguments:
  controller: passCtrl
  obscureText: true
  decoration: InputDecoration(labelText: "Password", ...)
  validator: (v) => (v == null || v.length < 6) ? 'Password must be 6+ characters' : null
Layout context:
  Parent: Column (11th child)

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 16.0
Layout context:
  Parent: Column (12th child)

WIDGET INSTANCE:
Type: TextFormField (Confirm Password)
Constructor arguments:
  controller: confirmCtrl
  obscureText: true
  decoration: InputDecoration(labelText: "Confirm Password", ...)
  validator: (v) => v != passCtrl.text ? 'Passwords do not match' : null
Layout context:
  Parent: Column (13th child)

WIDGET INSTANCE:
Type: AnimatedSize
Constructor arguments:
  duration: Duration(milliseconds: 300)
  child: role.value == 'driver' ? Column(...) : SizedBox.shrink()
Layout context:
  Parent: Column (14th child)
// WHY: Smoothly animates the appearance of the 'Vehicle Type' field when the 'Driver' role is selected.

WIDGET INSTANCE (Inside AnimatedSize):
Type: DropdownButtonFormField<String>
Constructor arguments:
  value: vehicleType.value
  decoration: InputDecoration(labelText: 'Vehicle Type', ...)
  items: [DropdownMenuItem(motorcycle), DropdownMenuItem(sedan), DropdownMenuItem(pickup), DropdownMenuItem(van), DropdownMenuItem(truck)]
  onChanged: (v) => vehicleType.value = v!
Layout context:
  Parent: Column inside AnimatedSize

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 40.0
Layout context:
  Parent: Column (15th child)

WIDGET INSTANCE:
Type: SizedBox (Button Wrapper)
Constructor arguments:
  width: double.infinity
  height: 56.0
  child: ElevatedButton(...)
Layout context:
  Parent: Column (16th child)

WIDGET INSTANCE:
Type: ElevatedButton
Constructor arguments:
  onPressed: loading.value ? null : signup
  style: ElevatedButton.styleFrom(
    backgroundColor: Color(0xFF0F1F91) (AppTheme.brandNavy),
    foregroundColor: Colors.white,
    shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),
  )
  child: loading.value ? CircularProgressIndicator(...) : Text("CONTINUE", ...)
Layout context:
  Parent: SizedBox

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 20.0
Layout context:
  Parent: Column (17th child)

WIDGET INSTANCE:
Type: TextButton
Constructor arguments:
  onPressed: () => context.pop()
  child: Text('Already have an account? Login', style: TextStyle(color: AppTheme.brandNavy, fontWeight: FontWeight.bold))
Layout context:
  Parent: Column (18th child)

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in signup():
  - if (!formKey.currentState!.validate()): Validates all fields.
  - loading.value = true: Sets loading state.
  - inal result = await ref.read(authServiceProvider).signUp(...): Calls auth service with extra driver-specific data if applicable.
  - loading.value = false: Resets loading state.
  - esult.when: Success redirects to /auth, failure shows snackbar.
- Navigation:
  - context.go('/auth'): On success.
  - context.pop(): On clicking login link.
- State Changes:
  - ole: Toggles between 'customer' and 'driver'.
  - ehicleType: Updates the selected vehicle for drivers.
  - loading: Controls UI feedback during the async signup process.

#### 5. LIFECYCLE AND CLEANUP
- useTextEditingController(): Automatic disposal.

CONTINUE TO NEXT FILE
=== FILE: lib/features/shared/profile_screen.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: ProfileScreen
  - Signature: class ProfileScreen extends ConsumerWidget
  - Fields: None
  - Methods:
    - Constructor: const ProfileScreen({super.key})
    - Method: build(BuildContext context, WidgetRef ref)
      - Parameters: context (BuildContext), ref (WidgetRef)
      - Return type: Widget
      - Local variables:
        - user (UserModel?): Obtained via ref.watch(currentUserDocProvider).value.

- Class: _InfoCard
  - Signature: class _InfoCard extends StatelessWidget
  - Fields:
    - final IconData icon
    - final String title
    - final String value
    - final Color? valueColor
  - Methods:
    - Constructor: const _InfoCard({required this.icon, required this.title, required this.value, this.valueColor})
    - Method: build(BuildContext context)

- Class: EditProfilePage
  - Signature: class EditProfilePage extends ConsumerStatefulWidget
  - Fields:
    - final dynamic user
  - Methods:
    - Constructor: const EditProfilePage({super.key, required this.user})
    - Method: createState() returns _EditProfilePageState

- Class: _EditProfilePageState
  - Signature: class _EditProfilePageState extends ConsumerState<EditProfilePage>
  - Fields:
    - final formKey = GlobalKey<FormState>()
    - late TextEditingController nameController
    - bool loading = false
  - Methods:
    - Method: initState()
    - Method: saveProfile()
    - Method: buildField({required String label, required TextEditingController controller, required IconData icon})
    - Method: build(BuildContext context)

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE (ProfileScreen.build - loading):
Type: Scaffold
Constructor arguments:
  backgroundColor: Colors.white
  body: Center(child: CircularProgressIndicator())
Layout context:
  Parent: ProfileScreen.build (if user == null)

WIDGET INSTANCE (ProfileScreen.build - data):
Type: Scaffold
Constructor arguments:
  backgroundColor: Colors.white
  body: SingleChildScrollView(...)
Layout context:
  Parent: ProfileScreen.build

WIDGET INSTANCE:
Type: SingleChildScrollView
Constructor arguments:
  child: Column(...)
Layout context:
  Parent: Scaffold

WIDGET INSTANCE:
Type: Column (Main)
Constructor arguments:
  children: [Container (Header), SizedBox, Padding (Info section)]
Layout context:
  Parent: SingleChildScrollView

WIDGET INSTANCE:
Type: Container (Header)
Constructor arguments:
  width: double.infinity
  padding: EdgeInsets.only(left: 24.0, top: 30.0, right: 24.0, bottom: 30.0) (EdgeInsets.symmetric(horizontal: 24, vertical: 30))
  decoration: BoxDecoration(
    color: Color(0xFF0F1F91) (AppTheme.brandNavy),
    borderRadius: BorderRadius.only(bottomLeft: Radius.circular(35), bottomRight: Radius.circular(35)),
  )
  child: Column(...)
Layout context:
  Parent: Column (Main) - 1st child
// WHY: Creates a distinctive, branded header section that visually separates user profile info from the rest of the content.

WIDGET INSTANCE (Inside Header Column):
Type: CircleAvatar
Constructor arguments:
  radius: 50.0
  backgroundColor: Colors.white
  backgroundImage: user.photoUrl != null ? NetworkImage(user.photoUrl!) : null
  child: user.photoUrl == null ? Icon(Icons.person, size: 45, color: Colors.grey) : null
Layout context:
  Parent: Column (Header) - 1st child

WIDGET INSTANCE (Inside Header Column):
Type: SizedBox
Constructor arguments:
  height: 14.0
Layout context:
  Parent: Column (Header) - 2nd child

WIDGET INSTANCE (Inside Header Column):
Type: Text (Name)
Constructor arguments:
  data: user.displayName
  style: TextStyle(color: Colors.white, fontSize: 22, fontWeight: FontWeight.bold)
Layout context:
  Parent: Column (Header) - 3rd child

WIDGET INSTANCE (Inside Header Column):
Type: SizedBox
Constructor arguments:
  height: 6.0
Layout context:
  Parent: Column (Header) - 4th child

WIDGET INSTANCE (Inside Header Column):
Type: Text (Email)
Constructor arguments:
  data: user.email
  style: TextStyle(color: Colors.white70, fontSize: 14)
Layout context:
  Parent: Column (Header) - 5th child

WIDGET INSTANCE (Inside Header Column):
Type: SizedBox
Constructor arguments:
  height: 14.0
Layout context:
  Parent: Column (Header) - 6th child

WIDGET INSTANCE (Inside Header Column):
Type: Container (Role Tag)
Constructor arguments:
  padding: EdgeInsets.only(left: 18.0, top: 8.0, right: 18.0, bottom: 8.0)
  decoration: BoxDecoration(
    color: user.role == 'customer' ? Color(0xFFFF8C42) (AppTheme.brandOrange variant) : Colors.white,
    borderRadius: BorderRadius.circular(30),
  )
  child: Text(user.role.toUpperCase(), style: TextStyle(color: user.role == 'customer' ? Colors.white : Color(0xFF0F1F91), fontWeight: FontWeight.bold))
Layout context:
  Parent: Column (Header) - 7th child
// WHY: Displays the user's role in a prominent, pill-shaped tag for quick identification.

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 24.0
Layout context:
  Parent: Column (Main) - 2nd child

WIDGET INSTANCE:
Type: Padding (Info section)
Constructor arguments:
  padding: EdgeInsets.only(left: 18.0, top: 0.0, right: 18.0, bottom: 0.0)
  child: Column(...)
Layout context:
  Parent: Column (Main) - 3rd child

WIDGET INSTANCE:
Type: _InfoCard (Email)
Constructor arguments:
  icon: Icons.email_outlined
  title: "Email"
  value: user.email
Layout context:
  Parent: Column (Info section) - 1st child

WIDGET INSTANCE (Conditional):
Type: _InfoCard (Vehicle)
Constructor arguments:
  icon: Icons.local_shipping_outlined
  title: "Vehicle"
  value: user.vehicleType?.toUpperCase() ?? "NOT SET"
Layout context:
  Parent: Column (Info section) - 2nd child (If user.role == 'driver')

WIDGET INSTANCE (Conditional):
Type: _InfoCard (Earnings)
Constructor arguments:
  icon: Icons.account_balance_wallet_outlined
  title: "Total Earnings"
  value: " EGP"
  valueColor: Color(0xFFFF8C42)
Layout context:
  Parent: Column (Info section) - 3rd child (If user.role == 'driver')

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 26.0
Layout context:
  Parent: Column (Info section) - 4th child

WIDGET INSTANCE:
Type: SizedBox (Edit Profile Button Wrapper)
Constructor arguments:
  width: double.infinity
  height: 52.0
  child: ElevatedButton.icon(...)
Layout context:
  Parent: Column (Info section) - 5th child

WIDGET INSTANCE:
Type: ElevatedButton.icon
Constructor arguments:
  onPressed: () { Navigator.push(context, MaterialPageRoute(builder: (_) => EditProfilePage(user: user))); }
  style: ElevatedButton.styleFrom(backgroundColor: Color(0xFF0F1F91), shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)))
  icon: Icon(Icons.edit, color: Colors.white)
  label: Text("Edit Profile", style: TextStyle(color: Colors.white, fontWeight: FontWeight.bold))
Layout context:
  Parent: SizedBox
// WHY: Primary action for editing user profile details.

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 14.0
Layout context:
  Parent: Column (Info section) - 6th child

WIDGET INSTANCE:
Type: SizedBox (Logout Button Wrapper)
Constructor arguments:
  width: double.infinity
  height: 52.0
  child: OutlinedButton.icon(...)
Layout context:
  Parent: Column (Info section) - 7th child

WIDGET INSTANCE:
Type: OutlinedButton.icon
Constructor arguments:
  onPressed: () async { await ref.read(authServiceProvider).signOut(); if (context.mounted) context.go('/login'); }
  style: OutlinedButton.styleFrom(side: BorderSide(color: Colors.red), shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)))
  icon: Icon(Icons.logout, color: Colors.red)
  label: Text("Logout", style: TextStyle(color: Colors.red, fontWeight: FontWeight.bold))
Layout context:
  Parent: SizedBox
// WHY: Red-styled logout button to indicate a destructive/session-ending action.

WIDGET INSTANCE (_InfoCard.build):
Type: Container
Constructor arguments:
  margin: EdgeInsets.only(bottom: 14.0)
  padding: EdgeInsets.only(left: 18.0, top: 18.0, right: 18.0, bottom: 18.0)
  decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(18), boxShadow: [BoxShadow(color: Color(0x0D000000), blurRadius: 10, offset: Offset(0, 4))])
  child: Row(...)
Layout context:
  Parent: _InfoCard.build
// WHY: Clean, card-based layout for displaying user information fields.

WIDGET INSTANCE (_InfoCard.build -> Row -> 1st child):
Type: Container (Icon Wrapper)
Constructor arguments:
  padding: EdgeInsets.all(12)
  decoration: BoxDecoration(color: Color(0x1A0F1F91), borderRadius: BorderRadius.circular(14))
  child: Icon(icon, color: Color(0xFF0F1F91))
Layout context:
  Parent: Row

WIDGET INSTANCE (_InfoCard.build -> Row -> 2nd child):
Type: SizedBox (Spacer)
Constructor arguments:
  width: 14.0
Layout context:
  Parent: Row

WIDGET INSTANCE (_InfoCard.build -> Row -> 3rd child):
Type: Expanded
Constructor arguments:
  child: Column(...)
Layout context:
  Parent: Row

WIDGET INSTANCE (_InfoCard.build -> Row -> 3rd child -> Column):
Type: Column
Constructor arguments:
  crossAxisAlignment: CrossAxisAlignment.start
  children: [Text(title), SizedBox, Text(value)]
Layout context:
  Parent: Expanded

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in ProfileScreen:
  - if (user == null): Displays loading indicator if user data isn't ready.
  - if (user.role == 'driver'): Conditionally renders vehicle and earnings cards.
- Control Flow in _EditProfilePageState.saveProfile():
  - if (!formKey.currentState!.validate()): Early return if form is invalid.
  - 	ry-catch: Handles the (currently placeholder) save logic and shows snackbars.
- Navigation:
  - Navigator.push(..., MaterialPageRoute(builder: (_) => EditProfilePage(user: user))): Navigates to edit page.
  - context.go('/login'): Navigates back to login after signing out.
- State Changes:
  - ef.watch(currentUserDocProvider): Rebuilds screen when user data updates.
  - loading (in _EditProfilePageState): Updates UI during the save operation.

#### 5. LIFECYCLE AND CLEANUP
- _EditProfilePageState.initState: Initializes 
ameController with current user's name.
- TextEditingController: Manual disposal should be added (currently missing in the provided code snippet, but standard practice).

CONTINUE TO NEXT FILE
=== FILE: lib/features/customer/customer_shell.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: CustomerShell
  - Signature: class CustomerShell extends ConsumerWidget
  - Fields:
    - final Widget child
  - Methods:
    - Constructor: const CustomerShell({super.key, required this.child})
    - Method: build(BuildContext context, WidgetRef ref)
      - Parameters: context (BuildContext), ref (WidgetRef)
      - Return type: Widget
      - Local variables:
        - location (String): Current URI path from GoRouter.
        - getCurrentIndex (function): Returns index for bottom nav based on location.
        - getTitle (function): Returns title for app bar based on location.
        - currentIndex (int): Result of getCurrentIndex().
        - title (String): Result of getTitle().

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  backgroundColor: Color(0xFFF5F7FB)
  appBar: PreferredSize(...)
  endDrawer: MoveItDrawer()
  body: AnimatedContainer(...)
  bottomNavigationBar: Container(...)
Layout context:
  Parent: CustomerShell.build
// WHY: Root layout for the customer section, managing persistent elements like AppBar, Drawer, and BottomNavigationBar.

WIDGET INSTANCE:
Type: PreferredSize
Constructor arguments:
  preferredSize: Size.fromHeight(65)
  child: AppBar(...)
Layout context:
  Parent: Scaffold.appBar

WIDGET INSTANCE:
Type: AppBar
Constructor arguments:
  backgroundColor: Color(0xFF0F1F91)
  elevation: 0.0
  centerTitle: false
  titleSpacing: 20.0
  title: title.isEmpty ? RichText(...) : Text(title, ...)
  iconTheme: IconThemeData(color: Colors.white)
  actions: [Builder(...)]
Layout context:
  Parent: PreferredSize
// WHY: Branded app bar with 'MoveIt' logo or page title.

WIDGET INSTANCE (Inside AppBar title - when empty):
Type: RichText
Constructor arguments:
  text: TextSpan(children: [TextSpan(text: 'Move', style: TextStyle(color: Colors.white, fontSize: 24, fontWeight: FontWeight.bold)), TextSpan(text: 'It', style: TextStyle(color: Color(0xFFFF8C42), fontSize: 24, fontWeight: FontWeight.bold))])
Layout context:
  Parent: AppBar.title

WIDGET INSTANCE (Inside AppBar actions):
Type: Builder
Constructor arguments:
  builder: (context) => IconButton(...)
Layout context:
  Parent: AppBar.actions

WIDGET INSTANCE (Inside Builder):
Type: IconButton
Constructor arguments:
  icon: Icon(Icons.menu_rounded)
  onPressed: () { Scaffold.of(context).openEndDrawer(); }
Layout context:
  Parent: Builder
// WHY: Opens the end drawer (MoveItDrawer).

WIDGET INSTANCE:
Type: MoveItDrawer
Constructor arguments:
  key: null
Layout context:
  Parent: Scaffold.endDrawer

WIDGET INSTANCE:
Type: AnimatedContainer
Constructor arguments:
  duration: Duration(milliseconds: 250)
  color: Color(0xFFF5F7FB)
  child: SafeArea(...)
Layout context:
  Parent: Scaffold.body
// WHY: Smoothly transitions background or layout properties if they change.

WIDGET INSTANCE:
Type: SafeArea
Constructor arguments:
  top: false
  child: child (The sub-page widget)
Layout context:
  Parent: AnimatedContainer

WIDGET INSTANCE:
Type: Container (Bottom Nav Wrapper)
Constructor arguments:
  decoration: BoxDecoration(color: Colors.white, boxShadow: [BoxShadow(color: Color(0x14000000), blurRadius: 18, offset: Offset(0, -4))])
  child: BottomNavigationBar(...)
Layout context:
  Parent: Scaffold.bottomNavigationBar
// WHY: Custom styling with shadow for the bottom navigation bar.

WIDGET INSTANCE:
Type: BottomNavigationBar
Constructor arguments:
  currentIndex: currentIndex
  type: BottomNavigationBarType.fixed
  backgroundColor: Colors.white
  elevation: 0.0
  selectedItemColor: Color(0xFFFF8C42)
  unselectedItemColor: Colors.grey.shade500
  selectedLabelStyle: TextStyle(fontWeight: FontWeight.w700, fontSize: 12)
  unselectedLabelStyle: TextStyle(fontWeight: FontWeight.w500, fontSize: 11)
  onTap: (index) { ... context.go(route) ... }
  items: [BottomNavigationBarItem(Home), BottomNavigationBarItem(Jobs), BottomNavigationBarItem(History), BottomNavigationBarItem(Profile)]
Layout context:
  Parent: Container

WIDGET INSTANCE (Inside BottomNavigationBarItem - Home):
Type: Container (Icon Wrapper)
Constructor arguments:
  padding: EdgeInsets.all(6)
  decoration: BoxDecoration(color: currentIndex == 0 ? Color(0x26FF8C42) : Colors.transparent, borderRadius: BorderRadius.circular(12))
  child: Icon(currentIndex == 0 ? Icons.home_rounded : Icons.home_outlined)
Layout context:
  Parent: BottomNavigationBarItem.icon

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in build():
  - getCurrentIndex(): Maps URI path to tab index.
  - getTitle(): Maps URI path to AppBar title.
  - onTap: Switches routes using context.go based on the selected index.
- Navigation:
  - context.go('/customer'): Home tab.
  - context.go('/customer/jobs'): Jobs tab.
  - context.go('/customer/history'): History tab.
  - context.go('/customer/profile'): Profile tab.
- State Changes:
  - Rebuilds whenever the GoRouterState changes (location changes).

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/features/customer/customer_home_screen.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: CustomerHomeScreen
  - Signature: class CustomerHomeScreen extends StatefulWidget
  - Methods:
    - Constructor: const CustomerHomeScreen({super.key})
    - Method: createState() returns _CustomerHomeScreenState

- Class: _CustomerHomeScreenState
  - Signature: class _CustomerHomeScreenState extends State<CustomerHomeScreen>
  - Fields:
    - bool isExpanded = false (Manages the expansion of the vehicle selection section).
  - Methods:
    - Method: build(BuildContext context)
    - Method: _buildVehicleBox({required BuildContext context, required String image, required String label})
    - Method: _walletAction({required IconData icon, required String label, required VoidCallback onTap})

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  backgroundColor: Colors.grey[50] (Color(0xFFFAFAFA))
  body: SingleChildScrollView(...)
Layout context:
  Parent: CustomerHomeScreen.build

WIDGET INSTANCE:
Type: SingleChildScrollView
Constructor arguments:
  padding: EdgeInsets.only(left: 16.0, top: 16.0, right: 16.0, bottom: 16.0)
  child: Column(...)
Layout context:
  Parent: Scaffold

WIDGET INSTANCE:
Type: Column (Main)
Constructor arguments:
  children: [Container (Vehicle Section), SizedBox, Container (Wallet Card), SizedBox, Container (Logistics Banner)]
Layout context:
  Parent: SingleChildScrollView

WIDGET INSTANCE (Vehicle Section Container):
Type: Container
Constructor arguments:
  decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(20), boxShadow: [BoxShadow(color: Color(0x0D000000), blurRadius: 15, offset: Offset(0, 6))])
  child: Column(...)
Layout context:
  Parent: Column (Main) - 1st child

WIDGET INSTANCE (Inside Vehicle Section):
Type: ListTile
Constructor arguments:
  leading: Container(width: 50, height: 50, decoration: BoxDecoration(color: Color(0xFFF5F5F5), borderRadius: BorderRadius.circular(12)), child: Icon(Icons.local_shipping, size: 32, color: AppTheme.brandNavy))
  title: Text("Choose your trip", style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold, color: Colors.black))
  subtitle: Text("Select your preferred delivery vehicle.", style: TextStyle(fontSize: 13, color: Colors.black))
  trailing: AnimatedRotation(turns: isExpanded ? 0.5 : 0, duration: Duration(milliseconds: 300), child: Icon(Icons.keyboard_arrow_down, color: Colors.black))
  onTap: () { setState(() { isExpanded = !isExpanded; }); }
Layout context:
  Parent: Column (Vehicle Section)
// WHY: Interactive header for selecting vehicle types. Uses AnimatedRotation for a smooth transition when expanding/collapsing.

WIDGET INSTANCE (Inside Vehicle Section - Conditional):
Type: Padding
Constructor arguments:
  padding: EdgeInsets.all(10.0)
  child: Column(children: [_buildVehicleBox(TruckSend), _buildVehicleBox(GoFast), _buildVehicleBox(FrozenGo), _buildVehicleBox(HeavyLoad), _buildVehicleBox(EasyGo)])
Layout context:
  Parent: Column (Vehicle Section) (If isExpanded is true)

WIDGET INSTANCE (_buildVehicleBox Result):
Type: InkWell
Constructor arguments:
  onTap: () => context.push('/customer/post-job')
  child: Container(...)
Layout context:
  Parent: Column inside Padding

WIDGET INSTANCE (Inside _buildVehicleBox -> Container):
Type: Container
Constructor arguments:
  height: 80.0
  margin: EdgeInsets.symmetric(vertical: 6)
  padding: EdgeInsets.symmetric(horizontal: 16)
  decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(12), border: Border.all(color: Colors.black12))
  child: Row(children: [Image.asset, SizedBox, Expanded(child: Text), Icon(Icons.chevron_right)])
Layout context:
  Parent: InkWell
// WHY: Visual representation of a vehicle option.

WIDGET INSTANCE (Wallet Card Container):
Type: Container
Constructor arguments:
  width: double.infinity
  padding: EdgeInsets.all(20)
  decoration: BoxDecoration(borderRadius: BorderRadius.circular(22), gradient: LinearGradient(colors: [Color(0xFF18398F), Color(0xFF142A5C)], begin: Alignment.topLeft, end: Alignment.bottomRight), boxShadow: [BoxShadow(color: Color(0x2E000000), blurRadius: 25, offset: Offset(0, 12))])
  child: Column(...)
Layout context:
  Parent: Column (Main) - 3rd child
// WHY: Premium-looking wallet card using a dark blue gradient to suggest security and value.

WIDGET INSTANCE (Inside Wallet Card - Balance):
Type: Text
Constructor arguments:
  data: "EGP 0.00"
  style: TextStyle(color: Colors.white, fontSize: 32, fontWeight: FontWeight.bold)
Layout context:
  Parent: Column (Wallet Card)

WIDGET INSTANCE (Inside Wallet Card - Actions Row):
Type: Row
Constructor arguments:
  mainAxisAlignment: MainAxisAlignment.spaceBetween
  children: [_walletAction(Top Up), _walletAction(Wallet), _walletAction(History)]
Layout context:
  Parent: Column (Wallet Card)

WIDGET INSTANCE (_walletAction Result):
Type: GestureDetector
Constructor arguments:
  onTap: onTap
  child: Column(children: [Container(Icon), SizedBox, Text(label)])
Layout context:
  Parent: Row

WIDGET INSTANCE (Logistics Banner Container):
Type: Container
Constructor arguments:
  width: double.infinity
  padding: EdgeInsets.all(20)
  decoration: BoxDecoration(borderRadius: BorderRadius.circular(20), gradient: LinearGradient(colors: [Color(0xFF0A3EC6), Color(0xFF0EC8EE)], begin: Alignment.topLeft, end: Alignment.bottomRight), boxShadow: [BoxShadow(color: Color(0x1F000000), blurRadius: 20, offset: Offset(0, 10))])
  child: Column(...)
Layout context:
  Parent: Column (Main) - 5th child
// WHY: Promotional banner using a vibrant blue-cyan gradient to catch the user's attention.

WIDGET INSTANCE (Inside Logistics Banner -> Book Now Button):
Type: Align
Constructor arguments:
  alignment: Alignment.bottomRight
  child: ElevatedButton(onPressed: () => context.push('/customer/post-job'), style: ElevatedButton.styleFrom(backgroundColor: Colors.white, foregroundColor: Colors.black, shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14))), child: Text("Book Now"))
Layout context:
  Parent: Column (Logistics Banner)

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in build():
  - isExpanded: Toggles the visibility of the vehicle selection list.
- Navigation:
  - context.push('/customer/post-job'): Triggered by clicking any vehicle box or the "Book Now" banner button.
  - Navigator.push(..., MaterialPageRoute(builder: (_) => CardScreen())): Top Up action.
  - Navigator.push(..., MaterialPageRoute(builder: (_) => WalletScreen())): Wallet/History actions.
- State Changes:
  - setState(() { isExpanded = !isExpanded; }): Rebuilds the UI to show/hide vehicle options.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/features/customer/customer_jobs_screen.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: CustomerJobsScreen
  - Signature: class CustomerJobsScreen extends ConsumerWidget
  - Fields: None
  - Methods:
    - Constructor: const CustomerJobsScreen({super.key})
    - Method: build(BuildContext context, WidgetRef ref)
      - Parameters: context (BuildContext), ref (WidgetRef)
      - Return type: Widget
      - Local variables:
        - jobsAsync (AsyncValue<List<JobModel>>): Watches customerJobsProvider.

- Class: _JobCard
  - Signature: class _JobCard extends StatelessWidget
  - Fields:
    - final JobModel job
  - Methods:
    - Constructor: const _JobCard({required this.job})
    - Method: build(BuildContext context)
      - Local variables:
        - currencyFormat (NumberFormat): Formats price to EGP.
        - dateStr (String): Formats job creation date.

- Class: _StatusChip
  - Signature: class _StatusChip extends StatelessWidget
  - Fields:
    - final String status
  - Methods:
    - Constructor: const _StatusChip({required this.status})
    - Method: build(BuildContext context)
      - Local variables:
        - color (Color): Derived from status.
        - icon (IconData): Derived from status.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  body: jobsAsync.when(...)
Layout context:
  Parent: CustomerJobsScreen.build

WIDGET INSTANCE (jobsAsync.when - loading):
Type: ListView.builder
Constructor arguments:
  itemCount: 5
  itemBuilder: (context, index) => JobCardShimmer()
Layout context:
  Parent: Scaffold.body
// WHY: Provides a shimmering placeholder while job data is being fetched from Firestore.

WIDGET INSTANCE (jobsAsync.when - data):
Type: ListView.builder
Constructor arguments:
  itemCount: jobs.length
  itemBuilder: (context, index) => _JobCard(job: jobs[index])
Layout context:
  Parent: Scaffold.body (If jobs is not empty)

WIDGET INSTANCE (jobsAsync.when - data - empty):
Type: Center
Constructor arguments:
  child: Text("You haven't posted any jobs yet.")
Layout context:
  Parent: Scaffold.body (If jobs is empty)

WIDGET INSTANCE (jobsAsync.when - error):
Type: ErrorStateWidget
Constructor arguments:
  message: e.toString()
  onRetry: () => ref.invalidate(customerJobsProvider)
Layout context:
  Parent: Scaffold.body
// WHY: Allows the user to manually trigger a refresh if the fetch fails.

WIDGET INSTANCE (_JobCard.build):
Type: Card
Constructor arguments:
  margin: EdgeInsets.symmetric(horizontal: 16.0, vertical: 8.0)
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16))
  child: ListTile(...)
Layout context:
  Parent: ListView.builder
// WHY: Encapsulates individual job information in a structured, shadow-elevated container.

WIDGET INSTANCE (_JobCard.build -> ListTile):
Type: ListTile
Constructor arguments:
  contentPadding: EdgeInsets.all(16.0)
  title: Text(job.itemDescription, maxLines: 1, overflow: TextOverflow.ellipsis, style: TextStyle(fontWeight: FontWeight.bold))
  subtitle: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [SizedBox, Row(children: [Icon(Icons.access_time), SizedBox, Text(dateStr)]), SizedBox, _StatusChip(status: job.status)])
  trailing: Column(mainAxisAlignment: MainAxisAlignment.center, crossAxisAlignment: CrossAxisAlignment.end, children: [Text(currencyFormat.format(job.pricePiastres / 100), ...), Icon(Icons.chevron_right)])
  onTap: () { /* TODO: Navigate to detail */ }
Layout context:
  Parent: Card

WIDGET INSTANCE (_StatusChip.build):
Type: Container
Constructor arguments:
  padding: EdgeInsets.symmetric(horizontal: 10.0, vertical: 4.0)
  decoration: BoxDecoration(color: color.withOpacity(0.1), borderRadius: BorderRadius.circular(12), border: Border.all(color: color.withOpacity(0.5)))
  child: Row(mainAxisSize: MainAxisSize.min, children: [Icon(icon, size: 12, color: color), SizedBox, Text(status.replaceAll('_', ' ').toUpperCase(), ...)])
Layout context:
  Parent: Column (inside _JobCard subtitle)
// WHY: Visually encodes the job status using color and icons for quick scanning.

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in build():
  - jobsAsync.when: Branches between data, loading, and error states.
- Control Flow in _StatusChip.build:
  - switch (status): Assigns specific colors and icons to statuses ('pending', 'accepted', 'in_transit', 'delivered').
- Navigation:
  - onTap (in _JobCard): Placeholder for navigation to CustomerJobDetailScreen.
- State Changes:
  - ef.watch(customerJobsProvider): Automatically rebuilds the list when the stream of customer-specific jobs emitted by Firestore changes.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/features/customer/customer_job_detail_screen.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: CustomerJobDetailScreen
  - Signature: class CustomerJobDetailScreen extends HookConsumerWidget
  - Fields:
    - final String jobId
  - Methods:
    - Constructor: const CustomerJobDetailScreen({super.key, required this.jobId})
    - Method: build(BuildContext context, WidgetRef ref)
      - Parameters: context (BuildContext), ref (WidgetRef)
      - Return type: Widget
      - Local variables:
        - job (ValueNotifier<JobModel?>): Managed by useState(null).
        - tracking (ValueNotifier<TrackingModel?>): Managed by useState(null).
        - mapController (MapController): Managed by useMemoized().

- Class: _StatusStep
  - Signature: class _StatusStep extends StatelessWidget
  - Fields:
    - final String label
    - final bool active
    - final bool done
  - Methods:
    - Constructor: const _StatusStep({required this.label, required this.active, required this.done})
    - Method: build(BuildContext context)

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  body: Stack(...)
Layout context:
  Parent: CustomerJobDetailScreen.build

WIDGET INSTANCE:
Type: Stack
Constructor arguments:
  children: [FlutterMap, Positioned (Back Button), DraggableScrollableSheet (Details)]
Layout context:
  Parent: Scaffold

WIDGET INSTANCE:
Type: FlutterMap
Constructor arguments:
  mapController: mapController
  options: MapOptions(initialCenter: LatLng(30.0444, 31.2357), initialZoom: 13.0)
  children: [TileLayer, MarkerLayer (Conditional)]
Layout context:
  Parent: Stack (1st child)
// WHY: Full-screen interactive map to visualize the delivery trip and real-time driver position.

WIDGET INSTANCE:
Type: TileLayer
Constructor arguments:
  urlTemplate: 'https://tile.openstreetmap.org/{z}/{x}/{y}.png'
  userAgentPackageName: 'com.moveit.egypt'
Layout context:
  Parent: FlutterMap

WIDGET INSTANCE (Conditional):
Type: MarkerLayer
Constructor arguments:
  markers: [Marker (Pickup), Marker (Dropoff), Marker (Driver - if tracking)]
Layout context:
  Parent: FlutterMap (If job.value != null)

WIDGET INSTANCE (Pickup Marker):
Type: Marker
Constructor arguments:
  point: LatLng(job.value!.pickupLatLng.latitude, job.value!.pickupLatLng.longitude)
  child: Icon(Icons.home, color: AppTheme.brandSkyBlue, size: 40.0)
Layout context:
  Parent: MarkerLayer

WIDGET INSTANCE (Dropoff Marker):
Type: Marker
Constructor arguments:
  point: LatLng(job.value!.dropoffLatLng.latitude, job.value!.dropoffLatLng.longitude)
  child: Icon(Icons.flag, color: AppTheme.brandOrange, size: 40.0)
Layout context:
  Parent: MarkerLayer

WIDGET INSTANCE (Driver Marker - Conditional):
Type: Marker
Constructor arguments:
  point: LatLng(tracking.value!.driverLatLng.latitude, tracking.value!.driverLatLng.longitude)
  rotate: true
  child: Transform.rotate(angle: tracking.value!.heading * (3.14159 / 180), child: Icon(Icons.local_shipping, color: AppTheme.brandNavy, size: 40.0))
Layout context:
  Parent: MarkerLayer (If tracking.value != null)
// WHY: Displays the driver's current location and heading (orientation) on the map.

WIDGET INSTANCE:
Type: Positioned (Back Button)
Constructor arguments:
  top: 50.0
  left: 16.0
  child: CircleAvatar(backgroundColor: Colors.white, child: IconButton(icon: Icon(Icons.arrow_back, color: AppTheme.brandNavy), onPressed: () => Navigator.pop(context)))
Layout context:
  Parent: Stack (2nd child)

WIDGET INSTANCE:
Type: DraggableScrollableSheet
Constructor arguments:
  initialChildSize: 0.35
  minChildSize: 0.15
  maxChildSize: 0.6
  builder: (context, scrollController) => Container(...)
Layout context:
  Parent: Stack (3rd child)
// WHY: Uber-style interactive sheet that allows the user to swipe up for more details or swipe down to see more of the map.

WIDGET INSTANCE (Inside DraggableScrollableSheet):
Type: Container
Constructor arguments:
  decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.vertical(top: Radius.circular(24)), boxShadow: [BoxShadow(blurRadius: 10, color: Colors.black12)])
  child: ListView(...)
Layout context:
  Parent: DraggableScrollableSheet

WIDGET INSTANCE (Inside Container -> ListView):
Type: ListView
Constructor arguments:
  controller: scrollController
  padding: EdgeInsets.all(24.0)
  children: [Center(child: Container(Handle)), SizedBox, Row(StatusTimeline), SizedBox, Text(Description - Conditional), SizedBox, Text(Vehicle - Conditional), Divider, Row(Driver Info - Conditional)]
Layout context:
  Parent: Container

WIDGET INSTANCE (Inside ListView -> StatusTimeline):
Type: Row
Constructor arguments:
  mainAxisAlignment: MainAxisAlignment.spaceBetween
  children: [_StatusStep(Order), _StatusStep(Pickup), _StatusStep(Transit), _StatusStep(Done)]
Layout context:
  Parent: ListView

WIDGET INSTANCE (_StatusStep.build):
Type: Column
Constructor arguments:
  children: [Container(Circle), SizedBox, Text(label)]
Layout context:
  Parent: Row

WIDGET INSTANCE (_StatusStep.build -> Container):
Type: Container
Constructor arguments:
  width: 24.0
  height: 24.0
  decoration: BoxDecoration(shape: BoxShape.circle, color: done ? AppTheme.brandSuccess : (active ? AppTheme.brandSkyBlue : Colors.grey[300]), border: Border.all(color: Colors.white, width: 4))
  child: done ? Icon(Icons.check, size: 12, color: Colors.white) : null
Layout context:
  Parent: Column

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in build():
  - if (job.value != null): Conditionally renders markers and details section.
  - if (tracking.value != null): Conditionally renders driver marker.
- Control Flow in _StatusStep.build:
  - done ? AppTheme.brandSuccess : (active ? AppTheme.brandSkyBlue : Colors.grey[300]): Determines the color of the status circle based on active/done state.
- Navigation:
  - Navigator.pop(context): Navigates back to the previous screen.
- State Changes:
  - job and 	racking: Local state variables (placeholders) that would normally be updated by listeners to Firestore streams.

#### 5. LIFECYCLE AND CLEANUP
- useMemoized(() => MapController()): Ensures the map controller is created once and reused.

CONTINUE TO NEXT FILE
=== FILE: lib/features/customer/post_job_screen.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: PostJobScreen
  - Signature: class PostJobScreen extends HookConsumerWidget
  - Methods:
    - Constructor: const PostJobScreen({super.key})
    - Method: build(BuildContext context, WidgetRef ref)
      - Parameters: context (BuildContext), ref (WidgetRef)
      - Return type: Widget
      - Local variables:
        - pageController (PageController): Managed by useMemoized().
        - currentStep (ValueNotifier<int>): Managed by useState(0).
        - descriptionCtrl (TextEditingController): Managed by useTextEditingController().
        - pickupLocation (ValueNotifier<LatLng?>): Managed by useState(null).
        - pickupAddress (ValueNotifier<String>): Managed by useState('').
        - dropoffLocation (ValueNotifier<LatLng?>): Managed by useState(null).
        - dropoffAddress (ValueNotifier<String>): Managed by useState('').
        - totalSteps (int): 4.
        - goNext (function): Moves to the next page in PageView.
        - goBack (function): Moves to the previous page or pops context.

- Class: _ItemDetailsStep
  - Signature: class _ItemDetailsStep extends StatelessWidget
  - Fields:
    - final TextEditingController controller
    - final VoidCallback onNext
  - Methods:
    - Constructor: const _ItemDetailsStep({required this.controller, required this.onNext})
    - Method: build(BuildContext context)

- Class: _LocationPickerStep
  - Signature: class _LocationPickerStep extends HookConsumerWidget
  - Fields:
    - final String title
    - final void Function(LatLng, String) onConfirmed
  - Methods:
    - Constructor: const _LocationPickerStep({required this.title, required this.onConfirmed})
    - Method: build(BuildContext context, WidgetRef ref)

- Class: _ReviewStep
  - Signature: class _ReviewStep extends StatelessWidget
  - Fields:
    - final String description, pickup, dropoff
    - final VoidCallback onPost
  - Methods:
    - Constructor: const _ReviewStep({required this.description, required this.pickup, required this.dropoff, required this.onPost})
    - Method: build(BuildContext context)

- Class: _ReviewRow
  - Signature: class _ReviewRow extends StatelessWidget
  - Fields:
    - final String label, value
    - final IconData icon
  - Methods:
    - Constructor: const _ReviewRow({required this.label, required this.value, required this.icon})
    - Method: build(BuildContext context)

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  backgroundColor: Colors.white
  appBar: AppBar(...)
  body: PageView(...)
Layout context:
  Parent: PostJobScreen.build

WIDGET INSTANCE:
Type: AppBar
Constructor arguments:
  backgroundColor: Color(0xFF0F1F91)
  foregroundColor: Colors.white
  elevation: 0.0
  leading: IconButton(icon: Icon(Icons.arrow_back), onPressed: goBack, color: Colors.white)
  title: Column(mainAxisSize: MainAxisSize.min, children: [Text('Post a Job — Step ...'), SizedBox, LinearProgressIndicator(...)])
  centerTitle: true
Layout context:
  Parent: Scaffold

WIDGET INSTANCE (Inside AppBar title):
Type: LinearProgressIndicator
Constructor arguments:
  value: (currentStep.value + 1) / totalSteps
  backgroundColor: Colors.white24
  valueColor: AlwaysStoppedAnimation(Colors.white)
  minHeight: 4.0
Layout context:
  Parent: Column inside AppBar
// WHY: Visual indicator of progress through the multi-step form.

WIDGET INSTANCE:
Type: PageView
Constructor arguments:
  controller: pageController
  physics: NeverScrollableScrollPhysics()
  children: [_ItemDetailsStep, _LocationPickerStep (Pickup), _LocationPickerStep (Dropoff), _ReviewStep]
Layout context:
  Parent: Scaffold
// WHY: Organizes the job posting process into logical steps. NeverScrollableScrollPhysics ensures navigation only happens via button actions for better control.

WIDGET INSTANCE (_ItemDetailsStep.build):
Type: Container
Constructor arguments:
  color: Colors.white
  padding: EdgeInsets.all(24.0)
  child: Column(crossAxisAlignment: CrossAxisAlignment.stretch, children: [SizedBox, Text(Question), SizedBox, Text(Hint), SizedBox, TextField(Description), Spacer, SizedBox(Button)])
Layout context:
  Parent: PageView (1st child)

WIDGET INSTANCE (_ItemDetailsStep.build -> TextField):
Type: TextField
Constructor arguments:
  controller: controller
  maxLines: 4
  maxLength: 200
  style: TextStyle(color: Colors.black, fontSize: 14)
  decoration: InputDecoration(labelText: 'Item description', hintText: 'e.g. Medium box of books...', filled: true, fillColor: Color(0xFFF2F2F2), border: OutlineInputBorder(...))
Layout context:
  Parent: Column
// WHY: Captures detailed information about the item being sent.

WIDGET INSTANCE (_LocationPickerStep.build):
Type: Stack
Constructor arguments:
  children: [FlutterMap, Center(Icon), Positioned(Search), Positioned(Confirm Panel)]
Layout context:
  Parent: PageView (2nd and 3rd children)

WIDGET INSTANCE (_LocationPickerStep.build -> FlutterMap):
Type: FlutterMap
Constructor arguments:
  mapController: mapController
  options: MapOptions(initialCenter: LatLng(30.0444, 31.2357), initialZoom: 14.0, onPositionChanged: (...) async { ... center.value = pos.center!; ... })
  children: [TileLayer]
Layout context:
  Parent: Stack

WIDGET INSTANCE (_LocationPickerStep.build -> Center -> Icon):
Type: Icon
Constructor arguments:
  icon: Icons.location_on
  size: 44.0
  color: Color(0xFF1265B3) (AppTheme.brandSkyBlue)
Layout context:
  Parent: Padding inside Center
// WHY: Fixed marker in the center of the map to indicate the selection point.

WIDGET INSTANCE (_LocationPickerStep.build -> Positioned -> Search Panel):
Type: Column
Constructor arguments:
  children: [Material(child: TextField(Search)), if (searchResults.isNotEmpty) Material(child: ListView.builder)]
Layout context:
  Parent: Positioned (top: 12, left: 12, right: 12)

WIDGET INSTANCE (_LocationPickerStep.build -> Positioned -> Confirm Panel):
Type: Container
Constructor arguments:
  padding: EdgeInsets.only(left: 24.0, top: 20.0, right: 24.0, bottom: 32.0)
  decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.vertical(top: Radius.circular(24)), boxShadow: [BoxShadow(...)])
  child: Column(children: [Text(title), SizedBox, Text(address.value), SizedBox, SizedBox(Button)])
Layout context:
  Parent: Positioned (bottom: 0)

WIDGET INSTANCE (_ReviewStep.build):
Type: Container
Constructor arguments:
  color: Colors.white
  padding: EdgeInsets.all(24.0)
  child: Column(crossAxisAlignment: CrossAxisAlignment.stretch, children: [Text('Review & Post'), SizedBox, Container(Review Box), Spacer, SizedBox(Button)])
Layout context:
  Parent: PageView (4th child)

WIDGET INSTANCE (_ReviewStep.build -> Review Box):
Type: Container
Constructor arguments:
  padding: EdgeInsets.all(20.0)
  decoration: BoxDecoration(color: Color(0xFF0F1F91), borderRadius: BorderRadius.circular(20), boxShadow: [BoxShadow(...)])
  child: Column(children: [_ReviewRow(Item), Divider, _ReviewRow(Pickup), Divider, _ReviewRow(Dropoff)])
Layout context:
  Parent: Column
// WHY: Summarizes the job details in a clear, high-contrast container for final review.

WIDGET INSTANCE (_ReviewRow.build):
Type: Padding
Constructor arguments:
  padding: EdgeInsets.symmetric(vertical: 10.0)
  child: Row(crossAxisAlignment: CrossAxisAlignment.start, children: [Icon, SizedBox, Expanded(child: Column)])
Layout context:
  Parent: Column inside Review Box

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in build():
  - goNext: Logic to advance to the next step.
  - goBack: Logic to retreat or exit.
- Control Flow in _LocationPickerStep.build:
  - onPositionChanged: Updates center.value and fetches the address via geocoding.reverse as the user pans the map.
  - onChanged (Search): Fetches autocomplete results via geocoding.search.
- Control Flow in _ReviewStep.onPost:
  - Validates uthUser.
  - Constructs a JobModel instance.
  - Calls createJob(job).
  - Navigates back to /customer on success.
- State Changes:
  - currentStep: Drives the stepper UI and progress bar.
  - pickupLocation, dropoffLocation, etc.: Accumulate job data across steps.

#### 5. LIFECYCLE AND CLEANUP
- useMemoized(() => PageController()): Manual disposal by Hook.
- useTextEditingController(): Manual disposal by Hook.

CONTINUE TO NEXT FILE
=== FILE: lib/features/driver/driver_shell.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: DriverShell
  - Signature: class DriverShell extends ConsumerWidget
  - Fields:
    - final Widget child
  - Methods:
    - Constructor: const DriverShell({super.key, required this.child})
    - Method: build(BuildContext context, WidgetRef ref)
      - Parameters: context (BuildContext), ref (WidgetRef)
      - Return type: Widget
      - Local variables:
        - location (String): Current URI path.
        - getCurrentIndex (function): Maps location to tab index.
        - getTitle (function): Maps location to page title.
        - title (String): Result of getTitle().

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  appBar: MoveItAppBar(title: title.isEmpty ? null : Text(title))
  endDrawer: MoveItDrawer()
  body: child (The sub-page widget)
  bottomNavigationBar: BottomNavigationBar(...)
Layout context:
  Parent: DriverShell.build

WIDGET INSTANCE:
Type: MoveItAppBar
Constructor arguments:
  title: title.isEmpty ? null : Text(title)
Layout context:
  Parent: Scaffold.appBar
// WHY: Specialized app bar that handles the 'MoveIt' logo logic when title is null.

WIDGET INSTANCE:
Type: MoveItDrawer
Constructor arguments:
  key: null
Layout context:
  Parent: Scaffold.endDrawer

WIDGET INSTANCE:
Type: BottomNavigationBar
Constructor arguments:
  currentIndex: getCurrentIndex()
  onTap: (index) { ... context.go(route) ... }
  selectedItemColor: Color(0xFFF79529) (AppTheme.brandOrange)
  unselectedItemColor: Colors.grey
  items: [BottomNavigationBarItem(Board), BottomNavigationBarItem(Active), BottomNavigationBarItem(History), BottomNavigationBarItem(Profile)]
Layout context:
  Parent: Scaffold.bottomNavigationBar
// WHY: Navigation specifically for driver functions (Job Board, Active Delivery, etc.).

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in build():
  - getCurrentIndex(): Logic to determine the active tab.
  - getTitle(): Logic to determine the header title.
  - onTap: Switches routes using context.go.
- Navigation:
  - context.go('/driver'): Board tab.
  - context.go('/driver/active'): Active Delivery tab.
  - context.go('/driver/history'): History tab.
  - context.go('/driver/profile'): Profile tab.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/features/driver/job_board_screen.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: JobBoardScreen
  - Signature: class JobBoardScreen extends HookConsumerWidget
  - Methods:
    - Constructor: const JobBoardScreen({super.key})
    - Method: build(BuildContext context, WidgetRef ref)
      - Parameters: context (BuildContext), ref (WidgetRef)
      - Return type: Widget
      - Local variables:
        - jobsAsync (AsyncValue<List<JobModel>>): Watches pendingJobsProvider.
        - mapController (MapController): Managed by useMemoized().

- Class: _JobBoardCard
  - Signature: class _JobBoardCard extends StatelessWidget
  - Fields:
    - final JobModel job
  - Methods:
    - Constructor: const _JobBoardCard({required this.job})
    - Method: build(BuildContext context)

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE (jobsAsync.when - loading):
Type: Scaffold
Constructor arguments:
  body: Center(child: CircularProgressIndicator())
Layout context:
  Parent: JobBoardScreen.build

WIDGET INSTANCE (jobsAsync.when - data):
Type: Scaffold
Constructor arguments:
  body: Stack(...)
Layout context:
  Parent: JobBoardScreen.build

WIDGET INSTANCE:
Type: Stack
Constructor arguments:
  children: [FlutterMap, DraggableScrollableSheet]
Layout context:
  Parent: Scaffold (data branch)

WIDGET INSTANCE:
Type: FlutterMap
Constructor arguments:
  mapController: mapController
  options: MapOptions(initialCenter: LatLng(30.0444, 31.2357), initialZoom: 12.0)
  children: [TileLayer, MarkerLayer]
Layout context:
  Parent: Stack (1st child)

WIDGET INSTANCE (Inside FlutterMap):
Type: MarkerLayer
Constructor arguments:
  markers: jobs.map((job) => Marker(point: LatLng(...), child: Icon(Icons.location_on, color: AppTheme.brandOrange, size: 30))).toList()
Layout context:
  Parent: FlutterMap
// WHY: Visualizes all available jobs as orange markers on the map for geographic awareness.

WIDGET INSTANCE:
Type: DraggableScrollableSheet
Constructor arguments:
  initialChildSize: 0.3
  minChildSize: 0.1
  maxChildSize: 0.8
  builder: (context, scrollController) => Container(...)
Layout context:
  Parent: Stack (2nd child)

WIDGET INSTANCE (Inside DraggableScrollableSheet):
Type: Container
Constructor arguments:
  decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.vertical(top: Radius.circular(24)), boxShadow: [BoxShadow(...)])
  child: jobs.isEmpty ? Center(...) : ListView.builder(...)
Layout context:
  Parent: DraggableScrollableSheet

WIDGET INSTANCE (Inside Container -> ListView.builder):
Type: ListView.builder
Constructor arguments:
  controller: scrollController
  padding: EdgeInsets.all(24.0)
  itemCount: jobs.length + 1
  itemBuilder: (context, index) { ... return index == 0 ? Header : _JobBoardCard ... }
Layout context:
  Parent: Container

WIDGET INSTANCE (_JobBoardCard.build):
Type: Card
Constructor arguments:
  margin: EdgeInsets.only(bottom: 16.0)
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16))
  child: Padding(padding: EdgeInsets.all(16.0), child: Column(...))
Layout context:
  Parent: ListView.builder
// WHY: Displays job summary details in a compact card format.

WIDGET INSTANCE (_JobBoardCard.build -> Column):
Type: Column
Constructor arguments:
  crossAxisAlignment: CrossAxisAlignment.start
  children: [Row(Description & Price), SizedBox, Row(Pickup Address), SizedBox, ElevatedButton(View Details)]
Layout context:
  Parent: Padding

WIDGET INSTANCE (Inside _JobBoardCard -> Price):
Type: Text
Constructor arguments:
  data: " EGP"
  style: TextStyle(color: Color(0xFFF79529) (AppTheme.brandOrange), fontWeight: FontWeight.bold, fontSize: 18)
Layout context:
  Parent: Row

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in build():
  - jobsAsync.when: Handles loading, error, and data states for the job stream.
  - jobs.isEmpty: Shows a placeholder message if no jobs match the driver's vehicle type.
- Navigation:
  - onPressed (View Details): Placeholder for navigation to detailed job view.
- State Changes:
  - ef.watch(pendingJobsProvider): Real-time updates as new jobs are posted.

#### 5. LIFECYCLE AND CLEANUP
- useMemoized(() => MapController()): Manual disposal by Hook.

CONTINUE TO NEXT FILE
=== FILE: lib/features/driver/active_job_screen.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: ActiveJobScreen
  - Signature: class ActiveJobScreen extends HookConsumerWidget
  - Methods:
    - Constructor: const ActiveJobScreen({super.key})
    - Method: build(BuildContext context, WidgetRef ref)
      - Parameters: context (BuildContext), ref (WidgetRef)
      - Return type: Widget
      - Local variables:
        - job (ValueNotifier<JobModel?>): Managed by useState(null).
        - mapController (MapController): Managed by useMemoized().

- Class: _StatusBadge
  - Signature: class _StatusBadge extends StatelessWidget
  - Fields:
    - final String status
  - Methods:
    - Constructor: const _StatusBadge({required this.status})
    - Method: build(BuildContext context)

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE (job.value == null):
Type: Scaffold
Constructor arguments:
  body: Center(child: Column(children: [Icon, SizedBox, Text, SizedBox, Text]))
Layout context:
  Parent: ActiveJobScreen.build (If no active job is found)
// WHY: Empty state UI to inform the driver they have no current assignment and should check the job board.

WIDGET INSTANCE (job.value != null):
Type: Scaffold
Constructor arguments:
  body: Stack(children: [FlutterMap, Positioned(Control Panel)])
Layout context:
  Parent: ActiveJobScreen.build (If an active job exists)

WIDGET INSTANCE:
Type: FlutterMap
Constructor arguments:
  mapController: mapController
  options: MapOptions(initialCenter: LatLng(30.0444, 31.2357), initialZoom: 14.0)
  children: [TileLayer, MarkerLayer]
Layout context:
  Parent: Stack (1st child)

WIDGET INSTANCE (Inside FlutterMap):
Type: MarkerLayer
Constructor arguments:
  markers: [Marker (Pickup), Marker (Dropoff)]
Layout context:
  Parent: FlutterMap
// WHY: Visualizes the start and end points of the current active delivery.

WIDGET INSTANCE:
Type: Positioned (Control Panel Wrapper)
Constructor arguments:
  bottom: 0.0, left: 0.0, right: 0.0
  child: Container(...)
Layout context:
  Parent: Stack (2nd child)

WIDGET INSTANCE (Inside Positioned -> Container):
Type: Container
Constructor arguments:
  padding: EdgeInsets.all(24.0)
  decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.vertical(top: Radius.circular(24)), boxShadow: [BoxShadow(...)])
  child: Column(mainAxisSize: MainAxisSize.min, children: [Row(Status & Price), SizedBox, Text(Description), SizedBox, Row(Customer Info), Divider, ActionButton (Conditional)])
Layout context:
  Parent: Positioned
// WHY: Central control interface for the driver to interact with the current job (e.g., mark as arrived, mark as delivered).

WIDGET INSTANCE (Conditional - status 'accepted'):
Type: ElevatedButton
Constructor arguments:
  onPressed: () {}
  style: ElevatedButton.styleFrom(backgroundColor: Color(0xFF1265B3) (AppTheme.brandSkyBlue))
  child: Text("I've Arrived at Pickup")
Layout context:
  Parent: Column inside Control Panel

WIDGET INSTANCE (Conditional - status 'in_transit'):
Type: ElevatedButton
Constructor arguments:
  onPressed: () {}
  style: ElevatedButton.styleFrom(backgroundColor: Color(0xFF2E7D32) (AppTheme.brandSuccess))
  child: Text("Mark as Delivered")
Layout context:
  Parent: Column inside Control Panel
// WHY: Context-aware actions that guide the driver through the delivery lifecycle.

WIDGET INSTANCE (_StatusBadge.build):
Type: Container
Constructor arguments:
  padding: EdgeInsets.symmetric(horizontal: 12.0, vertical: 4.0)
  decoration: BoxDecoration(color: Color(0x1A1265B3) (brandSkyBlue 10%), borderRadius: BorderRadius.circular(12))
  child: Text(status.toUpperCase(), style: TextStyle(color: AppTheme.brandSkyBlue, fontWeight: FontWeight.bold, fontSize: 12))
Layout context:
  Parent: Row inside Control Panel

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in build():
  - if (job.value == null): Early return for empty state.
  - if (job.value!.status == 'accepted') vs else if (job.value!.status == 'in_transit'): Displays the appropriate action button based on the current delivery stage.
- Navigation:
  - None within this screen (it's a shell route sub-page).
- State Changes:
  - job.value: Controls the entire UI. Updates would trigger marker changes and button updates.

#### 5. LIFECYCLE AND CLEANUP
- useMemoized(() => MapController()): Manual disposal by Hook.

CONTINUE TO NEXT FILE
=== FILE: lib/models/user_model.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: UserModel
  - Signature: class UserModel
  - Fields:
    - final String uid
    - final String role (Values: 'customer' | 'driver')
    - final String displayName
    - final String email
    - final String? photoUrl
    - final DateTime createdAt
    - final String? vehicleType (Optional, for drivers)
    - final int totalEarningsPiastres (Default: 0, for drivers)
    - final String? fcmToken (Optional, for notifications)
  - Methods:
    - Constructor: const UserModel({required this.uid, required this.role, required this.displayName, required this.email, this.photoUrl, required this.createdAt, this.vehicleType, this.totalEarningsPiastres = 0, this.fcmToken})
    - Getter: bool get isDriver: Returns true if role is 'driver'.
    - Getter: bool get isCustomer: Returns true if role is 'customer'.
    - Factory: UserModel.fromFirestore(DocumentSnapshot doc):
      - Description: Creates a UserModel from a Firestore DocumentSnapshot. Handles null values and type casting.
    - Method: Map<String, dynamic> toFirestore():
      - Description: Converts the UserModel into a Map suitable for Firestore storage. Uses FieldValue.serverTimestamp() for createdAt.
    - Method: UserModel copyWith({String? photoUrl, String? fcmToken, int? totalEarningsPiastres}):
      - Description: Returns a new UserModel instance with updated fields, preserving others.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(This is a data model, not a widget.)

#### 4. CONTROL FLOW AND STATE MAPPING
- Logic in fromFirestore:
  - (d['createdAt'] as Timestamp?)?.toDate() ?? DateTime.now(): Gracefully handles missing timestamps by defaulting to the current time.
- Logic in toFirestore:
  - if (photoUrl != null) 'photoUrl': photoUrl: Conditionally includes optional fields to keep Firestore documents clean.
// WHY: Ensures data consistency between the application and the Firestore backend. Using piastres (integers) for currency avoids floating-point precision issues in financial calculations.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/models/job_model.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: JobModel
  - Signature: class JobModel
  - Fields:
    - final String id
    - final String customerId
    - final String? driverId (Null until accepted)
    - final String status (Values: 'pending' | 'accepted' | 'in_transit' | 'delivered' | 'cancelled')
    - final GeoPoint pickupLatLng
    - final GeoPoint dropoffLatLng
    - final String pickupAddress
    - final String dropoffAddress
    - final String itemDescription
    - final String? itemPhotoUrl
    - final String vehicleTypeRequired
    - final int pricePiastres
    - final DateTime createdAt
    - final DateTime? acceptedAt
    - final DateTime? deliveredAt
  - Methods:
    - Constructor: const JobModel({required this.id, required this.customerId, this.driverId, required this.status, required this.pickupLatLng, required this.dropoffLatLng, required this.pickupAddress, required this.dropoffAddress, required this.itemDescription, this.itemPhotoUrl, required this.vehicleTypeRequired, required this.pricePiastres, required this.createdAt, this.acceptedAt, this.deliveredAt})
    - Getter: LatLng get pickupLatLng2: Converts Firestore GeoPoint to LatLng.
    - Getter: LatLng get dropoffLatLng2: Converts Firestore GeoPoint to LatLng.
    - Factory: JobModel.fromFirestore(DocumentSnapshot doc):
      - Description: Maps Firestore data to a JobModel instance.
    - Method: Map<String, dynamic> toFirestore():
      - Description: Maps JobModel to a Firestore-compatible Map.
    - Method: JobModel copyWith({String? driverId, String? status, DateTime? acceptedAt, DateTime? deliveredAt}):
      - Description: Returns a new instance with updated lifecycle fields.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(Data model).

#### 4. CONTROL FLOW AND STATE MAPPING
- Logic in romFirestore:
  - status: d['status'] as String? ?? 'pending': Defaults to pending if status is missing.
- Logic in 	oFirestore:
  - if (driverId != null) 'driverId': driverId: Only stores driverId once the job is accepted.
// WHY: Decoupling geographic types (GeoPoint vs LatLng) allows for easier integration with map libraries while maintaining Firestore compatibility.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/services/auth_service.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- Constant: firebaseAuthProvider
  - Type: Provider<FirebaseAuth>
  - Description: Provides the FirebaseAuth singleton instance.
- Constant: firestoreProvider
  - Type: Provider<FirebaseFirestore>
  - Description: Provides the FirebaseFirestore singleton instance.
- Constant: authStateChangesProvider
  - Type: StreamProvider<User?>
  - Description: Streams the current firebase user's authentication state changes.
- Constant: currentUserDocProvider
  - Type: StreamProvider<UserModel?>
  - Description: Streams the current user's profile document from the 'users' collection in Firestore, reactive to the auth state.
- Constant: authServiceProvider
  - Type: Provider<AuthService>
  - Description: Provides the AuthService instance, injecting dependencies.

#### 2. CLASS DECLARATIONS
- Class: AuthService
  - Signature: class AuthService
  - Fields:
    - final FirebaseAuth _auth
    - final FirebaseFirestore _db
  - Methods:
    - Constructor: AuthService(this._auth, this._db)
    - Getter: User? get currentUser: Returns current FirebaseAuth user.
    - Method: Future<Result<UserCredential, AppError>> signIn({required String email, required String password}):
      - Local variables: cred (UserCredential).
      - Description: Signs in a user using email and password. Returns a Result object (success/failure).
    - Method: Future<Result<UserCredential, AppError>> signUp({required String email, required String password, required String displayName, required String role, String? vehicleType}):
      - Local variables: cred (UserCredential), user (UserModel).
      - Description: Creates a new Firebase account and initializes their profile in the 'users' Firestore collection.
    - Method: Future<void> signOut(): Signs out the current user.
    - Static Method: String _mapAuthError(String code):
      - Description: Maps Firebase Auth error codes to user-friendly error messages.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(Backend service).

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in currentUserDocProvider:
  - uthState.when: 
    - data (user):
      - if (user == null) return Stream.value(null): If user is logged out, the stream emits null.
      - else: Sets up a listener on the user's Firestore document.
- Control Flow in signIn/signUp:
  - 	ry-catch: Catches FirebaseAuthException and maps the code to a friendly message.
  - Result.success / Result.failure: Encapsulates return values to avoid throwing exceptions in the UI layer.
- State Changes:
  - uthStateChanges(): The source of truth for the app's logged-in status.
  - Firestore Snapshots: Ensures the UI (like ProfileScreen) is always in sync with the database.

#### 5. LIFECYCLE AND CLEANUP
- StreamProviders are automatically managed by Riverpod; they cancel subscriptions when they are no longer watched.

CONTINUE TO NEXT FILE
=== FILE: lib/services/job_service.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- Constant: customerJobsProvider
  - Type: StreamProvider<List<JobModel>>
  - Description: Streams all jobs created by the current customer, ordered by creation date.
- Constant: pendingJobsProvider
  - Type: StreamProvider<List<JobModel>>
  - Description: Streams 'pending' jobs that match the current driver's vehicle type.
- Constant: activeJobProvider
  - Type: StreamProvider<JobModel?>
  - Description: Streams the job currently assigned to the driver (status 'accepted' or 'in_transit').
- Function: Future<bool> acceptJob(String jobId, String driverId):
  - Description: Uses a Firestore transaction to safely assign a job to a driver, ensuring it hasn't been taken by someone else simultaneously.
- Function: Future<void> createJob(JobModel job):
  - Description: Adds a new job document to the 'jobs' collection.

#### 2. CLASS DECLARATIONS
- None.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(Service).

#### 4. CONTROL FLOW AND STATE MAPPING
- Logic in cceptJob:
  - FirebaseFirestore.instance.runTransaction: Atomically reads the job status and updates it if it's still 'pending'.
  - if (!snap.exists || snap['status'] != 'pending') throw Exception(...): Prevents multiple drivers from accepting the same job.
- Logic in pendingJobsProvider:
  - where('status', isEqualTo: 'pending') and where('vehicleTypeRequired', isEqualTo: user.vehicleType): Filters the global job list to only show relevant work to the driver.
// WHY: Transactions are critical in a marketplace app to prevent race conditions during job acceptance.

#### 5. LIFECYCLE AND CLEANUP
- StreamSubscriptions are managed by Riverpod.

CONTINUE TO NEXT FILE
=== FILE: lib/services/geocoding_service.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- Constant: geocodingServiceProvider
  - Type: Provider<GeocodingService>
  - Description: Provides the GeocodingService instance.

#### 2. CLASS DECLARATIONS
- Class: GeocodingService
  - Signature: class GeocodingService
  - Fields:
    - static const _userAgent = 'MoveItApp/1.0 (com.moveit.egypt)'
  - Methods:
    - Method: Future<Result<List<Map<String, dynamic>>, AppError>> search(String query):
      - Description: Searches for addresses using Nominatim API. Constrained to Egypt (countrycodes=eg).
    - Method: Future<Result<String, AppError>> reverse(LatLng point):
      - Description: Converts coordinates to a human-readable address. Defaults to coordinate strings on failure.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(Service).

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in search:
  - if (response.statusCode == 200): Parses the JSON response into a list of results.
- Control Flow in everse:
  - catch (e) and if (response.statusCode != 200): Instead of failing, it returns the raw coordinates as a fallback string.
// WHY: Nominatim is used because it's free and sufficient for this project's requirements in Egypt, avoiding costly Google Maps API bills for simple geocoding.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/services/location_service.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- Constant: locationServiceProvider
  - Type: Provider<LocationService>
  - Description: Provides the LocationService instance.

#### 2. CLASS DECLARATIONS
- Class: LocationService
  - Signature: class LocationService
  - Methods:
    - Method: Future<Result<bool, AppError>> requestPermission():
      - Description: Checks for GPS status and requests location permissions from the user. Returns success if granted.
    - Method: void startTracking(String jobId):
      - Description: Subscribes to the device's location stream and periodically writes the driver's current position, heading, and timestamp to the 'tracking' collection in Firestore. Updates are filtered to occur only every 10 meters.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(Service).

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in equestPermission:
  - if (!await Geolocator.isLocationServiceEnabled()): Early return if GPS is disabled.
  - if (permission == LocationPermission.denied): Re-requests permission if previously denied but not permanently.
- Logic in startTracking:
  - listen((pos) { ... }): Continuous loop that updates Firestore as the driver moves.
// WHY: distanceFilter: 10 balances real-time tracking accuracy with battery efficiency and Firestore write costs.

#### 5. LIFECYCLE AND CLEANUP
- The location stream listener should be cancelled when tracking ends (currently relies on the listener implicitly being cleaned up by the system or the app closing, but explicit cancellation is a pending optimization).

CONTINUE TO NEXT FILE
=== FILE: lib/services/notification_service.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- Constant: notificationServiceProvider
  - Type: Provider<NotificationService>
  - Description: Provides the NotificationService instance.

#### 2. CLASS DECLARATIONS
- Class: NotificationService
  - Signature: class NotificationService
  - Fields:
    - final FirebaseMessaging _messaging
    - final FlutterLocalNotificationsPlugin _localNotifications
  - Methods:
    - Method: Future<void> init(String uid):
      - Local variables: androidSettings, iosSettings, token.
      - Description: Initializes push notification services. Requests permissions, initializes local notifications for foreground alerts, retrieves and persists the FCM token to Firestore, and sets up listeners for token refresh and incoming foreground messages.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(Service).

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in init:
  - wait _messaging.requestPermission(...): Blocks until the user responds to the OS permission prompt.
  - if (token != null): Only attempts to update Firestore if a valid token is retrieved.
- Logic in FirebaseMessaging.onMessage.listen:
  - if (notification != null): Checks if the incoming message contains a notification payload before attempting to show a local notification.
// WHY: FlutterLocalNotificationsPlugin is required to display notifications when the app is in the foreground, as Firebase does not show them by default on some platforms (like Android) while the app is active.

#### 5. LIFECYCLE AND CLEANUP
- FCM listeners are initialized once per user session (usually called after login).

CONTINUE TO NEXT FILE
=== FILE: lib/core/errors/app_error.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- Sealed Class: AppError
  - Description: Base class for all application-specific errors.
  - Factory Constructors: AppError.network, AppError.auth, AppError.firestore, AppError.storage, AppError.location, AppError.geocoding, AppError.validation, AppError.unknown.
  - Static Method: String mapMessage(AppError error):
    - Description: Pattern-matches the error type and returns a user-friendly string.

- Sealed Class: Result<T, E>
  - Description: A generic container for either a successful result (T) or a failure (E).
  - Factory Constructors: Result.success(T data), Result.failure(E error).
  - Method: R when<R>({required R Function(T data) success, required R Function(E error) failure}):
    - Description: Enables clean pattern-matching on the result object.
  - Getters: isSuccess, isFailure.

#### 2. CLASS DECLARATIONS
- All subclasses of AppError (e.g., NetworkError, AuthError, etc.) are declared as inal class and contain an optional String? message.
- Success<T, E> and Failure<T, E> are inal class implementations of Result<T, E>.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(Logic).

#### 4. CONTROL FLOW AND STATE MAPPING
- Logic in mapMessage:
  - switch (error) { ... }: Exhaustive switch ensures all error types provide a fallback message.
- Logic in Result.when:
  - switch (this) { Success(...) => ... }: Functional approach to handling return values from services.
// WHY: Using sealed classes and the Result pattern ensures that the UI layer handles all possible outcomes of an operation without resorting to 	ry-catch blocks everywhere, which improves code readability and safety.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/core/widgets/custom_snackbar.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- Enum: SnackBarType (Values: success, error, info).

#### 2. CLASS DECLARATIONS
- Class: CustomSnackBar
  - Signature: class CustomSnackBar
  - Methods:
    - Static Method: show(BuildContext context, {required String message, required SnackBarType type}):
      - Description: Displays a custom-styled, floating snackbar. Uses ScaffoldMessenger to manage the UI overlay.
    - Static Method: _getColor(SnackBarType type): Returns theme-specific color for the type.
    - Static Method: _getIcon(SnackBarType type): Returns theme-specific icon for the type.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: SnackBar
Constructor arguments:
  behavior: SnackBarBehavior.floating
  backgroundColor: Color(0xFF0F1F91) (AppTheme.brandNavy)
  elevation: 6.0
  margin: EdgeInsets.all(16.0)
  padding: EdgeInsets.zero
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))
  content: Container(...)
Layout context:
  Injected via ScaffoldMessenger.
// WHY: A floating snackbar with a navy background matches the app's premium aesthetic.

WIDGET INSTANCE (Inside SnackBar):
Type: Container
Constructor arguments:
  height: 60.0
  decoration: BoxDecoration(border: Border(left: BorderSide(color: color, width: 6.0)))
  padding: EdgeInsets.symmetric(horizontal: 16.0)
  child: Row(...)
Layout context:
  Parent: SnackBar
// WHY: The left border provides a colored 'accent' (green for success, red for error) that makes the type immediately obvious.

WIDGET INSTANCE (Inside Container):
Type: Row
Constructor arguments:
  children: [Icon(icon, color: color), SizedBox(width: 12), Expanded(child: Text(message, style: TextStyle(color: Colors.white, fontSize: 14)))]
Layout context:
  Parent: Container

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in show:
  - _getColor and _getIcon: Maps enum values to visual assets.
- State Changes:
  - Triggered by calling CustomSnackBar.show from any widget context.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/core/widgets/error_state_widget.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: ErrorStateWidget
  - Signature: class ErrorStateWidget extends StatelessWidget
  - Fields:
    - final String title
    - final String message
    - final VoidCallback onRetry
    - final String? lottieAsset
  - Methods:
    - Constructor: const ErrorStateWidget({super.key, this.title = 'Something went wrong', required this.message, required this.onRetry, this.lottieAsset})
    - Method: build(BuildContext context)

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Center
Constructor arguments:
  child: Padding(...)
Layout context:
  Commonly used as the body of a Scaffold or inside an AsyncValue.when error branch.

WIDGET INSTANCE:
Type: Padding
Constructor arguments:
  padding: EdgeInsets.all(24.0)
  child: Column(...)
Layout context:
  Parent: Center

WIDGET INSTANCE:
Type: Column
Constructor arguments:
  mainAxisSize: MainAxisSize.min
  children: [Lottie.asset OR Icon, SizedBox, Text(title), SizedBox, Text(message), SizedBox, ElevatedButton]
Layout context:
  Parent: Padding

WIDGET INSTANCE (Conditional - no lottie):
Type: Icon
Constructor arguments:
  icon: Icons.error_outline_rounded
  size: 80.0
  color: Color(0xFFD32F2F) (AppTheme.brandError)
Layout context:
  Parent: Column (1st child)

WIDGET INSTANCE:
Type: Text (title)
Constructor arguments:
  data: title
  style: Theme.of(context).textTheme.headlineMedium
  textAlign: TextAlign.center
Layout context:
  Parent: Column (3rd child)

WIDGET INSTANCE:
Type: Text (message)
Constructor arguments:
  data: message
  style: Theme.of(context).textTheme.bodyLarge?.copyWith(color: Colors.grey[600])
  textAlign: TextAlign.center
Layout context:
  Parent: Column (5th child)

WIDGET INSTANCE:
Type: ElevatedButton
Constructor arguments:
  onPressed: onRetry
  child: Text('Try Again')
Layout context:
  Parent: Column (7th child)
// WHY: Centralizes error handling UI to ensure consistency. Allows for Lottie animations to provide a more refined UX when available.

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in build():
  - if (lottieAsset != null): Switches between a static icon and a Lottie animation.
- State Changes:
  - Invokes the onRetry callback when pressed, typically triggering a provider invalidation or a service retry.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/core/widgets/job_card_shimmer.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: JobCardShimmer
  - Signature: class JobCardShimmer extends StatelessWidget
  - Methods:
    - Constructor: const JobCardShimmer({super.key})
    - Method: build(BuildContext context)

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Padding
Constructor arguments:
  padding: EdgeInsets.symmetric(horizontal: 16.0, vertical: 8.0)
  child: Shimmer.fromColors(...)
Layout context:
  Used inside a ListView during loading states.

WIDGET INSTANCE:
Type: Shimmer.fromColors
Constructor arguments:
  baseColor: Colors.grey[300]!
  highlightColor: Colors.grey[100]!
  child: Container(...)
Layout context:
  Parent: Padding
// WHY: Provides a shimmering animation to indicate content is being loaded, which is more engaging than a simple spinner.

WIDGET INSTANCE (Inside Shimmer):
Type: Container
Constructor arguments:
  height: 120.0
  decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(16))
Layout context:
  Parent: Shimmer
// WHY: Mimics the shape and size of a standard _JobCard to maintain layout stability during loading.

#### 4. CONTROL FLOW AND STATE MAPPING
- None (purely visual component).

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/core/widgets/moveit_app_bar.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: MoveItAppBar
  - Signature: class MoveItAppBar extends StatelessWidget implements PreferredSizeWidget
  - Fields:
    - final bool showMenu
    - final Widget? title
  - Methods:
    - Constructor: const MoveItAppBar({super.key, this.showMenu = true, this.title})
    - Method: build(BuildContext context)
    - Getter: Size get preferredSize: Returns Size.fromHeight(kToolbarHeight).

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: AppBar
Constructor arguments:
  backgroundColor: Color(0xFF0F1F91) (AppTheme.brandNavy)
  elevation: 4.0
  title: title ?? RichText(...)
  centerTitle: false
  actions: [if (showMenu) Builder(...)]
Layout context:
  Used as the appBar of a Scaffold.
// WHY: Centralized AppBar implementation ensures consistent branding and behavior (like opening the end drawer) across different screens.

WIDGET INSTANCE (Inside AppBar title - default):
Type: RichText
Constructor arguments:
  text: TextSpan(children: [TextSpan(text: 'M', style: TextStyle(color: Color(0xFF1265B3), ...)), TextSpan(text: 'ove', style: TextStyle(color: Colors.lightBlueAccent, ...)), TextSpan(text: 'It', style: TextStyle(color: Colors.yellow, ...))])
Layout context:
  Parent: AppBar.title
// WHY: Multi-colored 'MoveIt' logo implemented using RichText for fine-grained styling control.

WIDGET INSTANCE (Inside AppBar actions):
Type: Builder
Constructor arguments:
  builder: (context) => IconButton(...)
Layout context:
  Parent: AppBar.actions (If showMenu is true)

WIDGET INSTANCE (Inside Builder):
Type: IconButton
Constructor arguments:
  icon: Icon(Icons.menu, color: Colors.yellow)
  iconSize: 32.0
  onPressed: () { Scaffold.of(context).openEndDrawer(); }
Layout context:
  Parent: Builder
// WHY: Consistent menu icon that triggers the app's global drawer. Yellow color provides high visibility against the navy background.

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in build():
  - 	itle ?? RichText(...): Defaults to the logo if no title is provided.
  - if (showMenu): Conditionally shows the drawer trigger.
- State Changes:
  - Scaffold.of(context).openEndDrawer(): Interacts with the parent Scaffold state.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/core/widgets/moveit_drawer.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: MoveItDrawer
  - Signature: class MoveItDrawer extends ConsumerStatefulWidget
  - Methods:
    - Constructor: const MoveItDrawer({super.key})
    - Method: createState() returns _MoveItDrawerState

- Class: _MoveItDrawerState
  - Signature: class _MoveItDrawerState extends ConsumerState<MoveItDrawer>
  - Methods:
    - Method: build(BuildContext context)
      - Local variables: user (UserModel?).
    - Method: _buildTile({required IconData icon, required String title, required VoidCallback onTap, Widget? trailing, Color? titleColor, Color? iconColor}):
      - Description: Helper method to build consistent menu items.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Drawer
Constructor arguments:
  backgroundColor: Color(0xFFF8F9FD)
  child: SafeArea(...)
Layout context:
  Managed by a Scaffold's endDrawer property.

WIDGET INSTANCE:
Type: SafeArea
Constructor arguments:
  child: Column(children: [Container(Header), SizedBox, Expanded(child: ListView(Menu))])
Layout context:
  Parent: Drawer

WIDGET INSTANCE (Drawer Header):
Type: Container
Constructor arguments:
  width: double.infinity
  padding: EdgeInsets.symmetric(horizontal: 20, vertical: 24)
  decoration: BoxDecoration(color: Color(0xFF0F1F91), borderRadius: BorderRadius.only(bottomLeft: Radius.circular(28), bottomRight: Radius.circular(28)))
  child: Column(children: [CircleAvatar, SizedBox, Text(DisplayName), SizedBox, Text(Email), SizedBox, Container(Role Tag)])
Layout context:
  Parent: Column (1st child)
// WHY: Distinct header section that mirrors the ProfileScreen aesthetic, reinforcing the app's brand identity.

WIDGET INSTANCE (Inside Header -> Role Tag):
Type: Container
Constructor arguments:
  padding: EdgeInsets.symmetric(horizontal: 16, vertical: 6)
  decoration: BoxDecoration(color: Color(0xFFFF8C42), borderRadius: BorderRadius.circular(30))
  child: Text(user.role.toUpperCase(), style: TextStyle(color: Colors.white, fontWeight: FontWeight.bold, fontSize: 11))
Layout context:
  Parent: Column

WIDGET INSTANCE (Drawer Menu):
Type: ListView
Constructor arguments:
  padding: EdgeInsets.symmetric(horizontal: 14)
  children: [_buildTile(Profile), _buildTile(Jobs), _buildTile(History), _buildTile(Settings)]
Layout context:
  Parent: Expanded

WIDGET INSTANCE (_buildTile Result):
Type: Container
Constructor arguments:
  margin: EdgeInsets.only(bottom: 12)
  decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(16), boxShadow: [BoxShadow(color: Color(0x0A000000), blurRadius: 10)])
  child: ListTile(leading: Icon, title: Text, trailing: Icon, onTap: onTap)
Layout context:
  Parent: ListView
// WHY: Clean, card-based list items for menu navigation.

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in build():
  - user?.photoUrl != null: Switches between network image and person icon for avatar.
  - user?.displayName ?? "MoveIt User": Fallback for missing user data.
- Navigation:
  - Navigator.pop(context): Closes the drawer before performing any navigation.
  - context.go('/customer/profile'): Home tab.
  - context.go('/customer/jobs'): Jobs tab.
  - context.go('/customer/history'): History tab.
  - Navigator.push(..., MaterialPageRoute(builder: (_) => SettingsPage())): Opens settings.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/core/widgets/settings.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: SettingsPage
  - Signature: class SettingsPage extends StatefulWidget
  - Fields:
    - static const String routeName = "settings"
  - Methods:
    - Constructor: const SettingsPage({super.key})
    - Method: createState() returns _SettingsPageState

- Class: _SettingsPageState
  - Signature: class _SettingsPageState extends State<SettingsPage>
  - Fields:
    - String name = "Seif Ahmed" (Hardcoded placeholder)
    - String address = "Cairo, Egypt" (Hardcoded placeholder)
    - String selectedLanguage = "English"
  - Methods:
    - Method: build(BuildContext context)
    - Method: _card({required Widget child}): Helper for building white rounded containers.
    - Method: _tile(IconData icon, String title, VoidCallback onTap, {Widget? trailing, Color? color}): Helper for building settings list items.
    - Method: _showLanguageDialog(BuildContext context): Shows a language selection modal.
    - Method: _showLogoutDialog(BuildContext context): Shows a logout confirmation modal.
    - Method: _showDeleteDialog(BuildContext context): Shows an account deletion confirmation modal.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  backgroundColor: Color(0xFFF5F7FB)
  appBar: AppBar(backgroundColor: Color(0xFF0F1F91), title: Text("Settings"), centerTitle: true)
  body: ListView(...)
Layout context:
  Parent: _SettingsPageState.build

WIDGET INSTANCE (Inside ListView -> Profile Card):
Type: _card Result
Constructor arguments:
  child: Row(children: [Container(Avatar), SizedBox, Expanded(child: Column(Name & Address))])
Layout context:
  Parent: ListView (1st child)
// WHY: Displays a summary of the user's profile at the top of the settings list.

WIDGET INSTANCE (Inside Profile Card -> Avatar):
Type: Container
Constructor arguments:
  width: 56.0, height: 56.0
  decoration: BoxDecoration(color: Color(0xFF0F1F91), borderRadius: BorderRadius.circular(14))
  child: Center(child: Text("SA", ...))
Layout context:
  Parent: Row
// WHY: Fallback avatar showing user initials.

WIDGET INSTANCE (Settings Tiles):
Type: _tile Result
Constructor arguments:
  (Payment): Icons.payment, "Payment", () => Navigate to WalletScreen
  (Language): Icons.language, "Language", () => _showLanguageDialog, trailing: Text(selectedLanguage)
  (Privacy): Icons.privacy_tip_outlined, "Privacy Policy"
  (Version): Icons.info_outline, "App Version", trailing: Text("1.0.0")
  (Logout): Icons.logout, "Logout", () => _showLogoutDialog, color: Colors.red
  (Delete): Icons.delete_forever, "Delete My Account", () => _showDeleteDialog, color: Colors.red
Layout context:
  Parent: ListView

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in _showLanguageDialog:
  - setState(() => selectedLanguage = ...): Rebuilds the UI to update the displayed language name.
- Control Flow in _showLogoutDialog:
  - Navigator.pushAndRemoveUntil(..., (route) => false): Clears the navigation stack and returns the user to the login screen.
- State Changes:
  - Local state management for selectedLanguage.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/models/tracking_model.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: TrackingModel
  - Signature: class TrackingModel
  - Fields:
    - final GeoPoint driverLatLng
    - final DateTime updatedAt
    - final double heading (Rotation of the vehicle on the map)
  - Methods:
    - Constructor: const TrackingModel({required this.driverLatLng, required this.updatedAt, this.heading = 0.0})
    - Getter: LatLng get latLng: Helper to convert GeoPoint to map-compatible LatLng.
    - Factory: TrackingModel.fromFirestore(DocumentSnapshot doc):
      - Description: Maps live tracking data from Firestore to a model. Defaults updatedAt to now and heading to 0 if missing.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(Data model).

#### 4. CONTROL FLOW AND STATE MAPPING
- Logic in romFirestore:
  - (d['heading'] as num?)?.toDouble() ?? 0.0: Safely handles both integers and doubles from Firestore to ensure a double result.
// WHY: Separate model for tracking data avoids bloated JobModel objects and allows for frequent, lightweight updates to the map UI.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/core/constants/app_constants.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: AppConstants
  - Signature: class AppConstants
  - Fields:
    - static const double cairoLat = 30.0444
    - static const double cairoLng = 31.2357
    - static const LatLng cairoCenter = LatLng(cairoLat, cairoLng)
    - static const double defaultMapZoom = 13.0
    - static const List<String> vehicleTypes = ['motorcycle', 'sedan', 'pickup_truck', 'van', 'large_truck']
    - static const String statusPending = 'pending'
    - static const String statusAccepted = 'accepted'
    - static const String statusInTransit = 'in_transit'
    - static const String statusDelivered = 'delivered'
    - static const String statusCancelled = 'cancelled'
    - static const List<Map<String, dynamic>> _cairoDistricts = [Maadi, Zamalek, Downtown, Heliopolis, Nasr City, New Cairo, Giza, Dokki, Mohandessin, 6th October] (With bounding box coordinates).
  - Methods:
    - Private Constructor: AppConstants._(): Prevents instantiation.
    - Static Method: String formatPiastres(int piastres):
      - Description: Converts integer piastres to a human-readable 'EGP 0.00' string.
    - Static Method: int egpToPiastres(double egp):
      - Description: Converts a double EGP value to integer piastres, rounding correctly.
    - Static Method: String vehicleLabel(String type):
      - Description: Returns a user-friendly display name for a vehicle type (e.g., 'motorcycle' -> 'Motorcycle').
    - Static Method: String vehicleIcon(String type):
      - Description: Returns the Material Icon name associated with a vehicle type.
    - Static Method: String getDistrict(double lat, double lng):
      - Description: Performs a local bounding-box search to identify which Cairo district a coordinate belongs to.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(Constants).

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in getDistrict:
  - or (final d in _cairoDistricts): Iterates through defined geographic bounds. If a coordinate falls within a box, returns the district name. Defaults to 'Cairo'.
// WHY: Centralizing geographic and financial logic ensures that formatting and calculations are consistent throughout the app, whether in a job card or on the profile screen.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/core/constants/firestore_schema.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- Note: This file contains conceptual schema definitions and Security Rules in comment blocks.

#### 2. CLASS DECLARATIONS
- None.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY
(Documentation).

#### 4. CONTROL FLOW AND STATE MAPPING
- Schema Summary:
  - users/{uid}: Stores roles, identity, and driver-specific attributes (earnings, vehicle).
  - jobs/{jobId}: Stores state machine data for delivery requests (locations, pricing, timing).
  - 	racking/{jobId}: Stores high-frequency updates for active delivery visualization.
- Security Rules Logic:
  - Users can read all profiles but only write their own.
  - Jobs are readable by all authenticated users.
  - Only customers can create jobs.
  - Job updates are allowed for the involved customer/driver, or any driver accepting a 'pending' job.
  - Tracking writes are restricted to the driver assigned to that specific job.
// WHY: Documenting the schema and rules here provides a single source of truth for both the frontend data models and the backend configuration.

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE
=== FILE: lib/features/customer/cardscreen.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: CardScreen
  - Signature: class CardScreen extends StatefulWidget
  - Fields:
    - static const String routeName = '/card'
  - Methods:
    - Constructor: const CardScreen({super.key})
    - Method: createState() returns _CardScreenState

- Class: _CardScreenState
  - Signature: class _CardScreenState extends State<CardScreen>
  - Fields:
    - final TextEditingController cardController, dateController, cvvController
  - Methods:
    - Method: _addCard(): Validates inputs and navigates to WalletScreen.
    - Method: build(BuildContext context)
    - Method: dispose(): Cleans up text controllers.

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  backgroundColor: Color(0xFFF5F7FB)
  appBar: AppBar(backgroundColor: Color(0xFF0F1F91), title: Text("Add Card"), centerTitle: true)
  body: Padding(...)
Layout context:
  Parent: _CardScreenState.build

WIDGET INSTANCE:
Type: Padding
Constructor arguments:
  padding: EdgeInsets.all(16.0)
  child: Column(children: [Container(Header), SizedBox, TextField(CardNumber), SizedBox, Row(Date & CVV), Spacer, SizedBox(Button)])
Layout context:
  Parent: Scaffold

WIDGET INSTANCE:
Type: TextField (Card Number)
Constructor arguments:
  controller: cardController
  keyboardType: TextInputType.number
  decoration: InputDecoration(labelText: "Card Number", prefixIcon: Icon(Icons.credit_card), filled: true, fillColor: Colors.white, border: OutlineInputBorder(...))
Layout context:
  Parent: Column

WIDGET INSTANCE:
Type: Row
Constructor arguments:
  children: [Expanded(TextField(MM/YY)), SizedBox, Expanded(TextField(CVV))]
Layout context:
  Parent: Column

WIDGET INSTANCE:
Type: ElevatedButton
Constructor arguments:
  onPressed: _addCard
  style: ElevatedButton.styleFrom(backgroundColor: Colors.orange, shape: RoundedRectangleBorder(...))
  child: Text("Add Card")
Layout context:
  Parent: Column

#### 4. CONTROL FLOW AND STATE MAPPING
- Control Flow in _addCard:
  - if (card.isEmpty || date.isEmpty || cvv.isEmpty): Triggers a SnackBar for validation.
- Navigation:
  - Navigator.pushNamed(context, WalletScreen.routeName): Navigates to the wallet overview.

#### 5. LIFECYCLE AND CLEANUP
- dispose(): Properly closes text controllers to prevent memory leaks.

CONTINUE TO NEXT FILE
=== FILE: lib/features/customer/walletscreen.dart ===

#### 1. TOP-LEVEL DECLARATIONS
- None.

#### 2. CLASS DECLARATIONS
- Class: WalletScreen
  - Signature: class WalletScreen extends StatelessWidget
  - Fields:
    - static const String routeName = '/wallet'
  - Methods:
    - Constructor: const WalletScreen({super.key})
    - Method: build(BuildContext context)

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  backgroundColor: Colors.white
  appBar: AppBar(backgroundColor: Color(0xFF0F1F91), title: Text('Wallet'), centerTitle: true)
  body: Column(...)
Layout context:
  Parent: WalletScreen.build

WIDGET INSTANCE (Balance Box):
Type: Container
Constructor arguments:
  padding: EdgeInsets.all(15.0)
  decoration: BoxDecoration(color: Color(0xFF0F1F91), borderRadius: BorderRadius.circular(12))
  child: Column(children: [Text('Balance'), Row(children: [Text('EGP '), Text('100.50'), Icon(...)])])
Layout context:
  Parent: Column (1st child)

WIDGET INSTANCE (Payment Methods Section):
Type: Column
Constructor arguments:
  children: [Text('Payment Methods'), Container(Cash Tile), InkWell(Add Card Button), Text('Saved Cards'), Container(Card Tile)]
Layout context:
  Parent: Main Column

WIDGET INSTANCE (Cash Tile):
Type: Container
Constructor arguments:
  padding: EdgeInsets.all(12.0)
  decoration: BoxDecoration(color: Colors.orange.shade50, borderRadius: BorderRadius.circular(12), border: Border.all(color: Colors.orange))
  child: Row(children: [Icon(Icons.money, color: Colors.orange), SizedBox, Text("Cash")])
Layout context:
  Parent: Main Column

WIDGET INSTANCE (Add Card Button):
Type: InkWell
Constructor arguments:
  onTap: () => Navigator.push(..., MaterialPageRoute(builder: (_) => CardScreen()))
  child: Container(decoration: BoxDecoration(color: Color(0xFF0F1F91), ...), child: Row(children: [Icon(Icons.add_card), ...]))
Layout context:
  Parent: Main Column

#### 4. CONTROL FLOW AND STATE MAPPING
- Navigation:
  - Navigator.push(..., MaterialPageRoute(builder: (_) => CardScreen())): Navigates to the add card form.
- State:
  - Purely static placeholder data (100.50 balance, **** 1234 card).

#### 5. LIFECYCLE AND CLEANUP
- None.

CONTINUE TO NEXT FILE

=== NOTE: DUPLICATE SOURCE DIRECTORIES ===
The following directories appear to contain duplicate or legacy versions of the application source code. The primary codebase is located within lib/features/, as referenced by the pp_router.dart.
- lib/auth/ (Duplicate of lib/features/auth/)
- lib/customer/ (Duplicate of lib/features/customer/)
- lib/driver/ (Duplicate of lib/features/driver/)
- lib/shared/ (Duplicate of lib/features/shared/)

=== INVENTORY COMPLETE ===
Cwd:

=== DETAILED EXPANSION UPDATES ===

=== FILE: lib/features/customer/customer_home_screen.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  backgroundColor: Colors.grey[50]
  body: SingleChildScrollView(...)
Layout context:
  Parent: CustomerHomeScreen.build
// WHY: Provides the primary visual structure for the home screen, using a light grey background to distinguish from white card components.

WIDGET INSTANCE:
Type: SingleChildScrollView
Constructor arguments:
  padding: const EdgeInsets.only(left: 16.0, top: 16.0, right: 16.0, bottom: 16.0)
  child: Column(...)
Layout context:
  Parent: Scaffold.body
// WHY: Allows the dashboard content to be scrollable on smaller devices or when the vehicle list is expanded.

WIDGET INSTANCE:
Type: Column
Constructor arguments:
  children: [Container (Vehicle Section), SizedBox, Container (Wallet Card), SizedBox, Container (Logistics Banner)]
Layout context:
  Parent: SingleChildScrollView.child
// WHY: Arranges the main dashboard modules vertically.

WIDGET INSTANCE:
Type: Container (Vehicle Section)
Constructor arguments:
  decoration: BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.circular(20),
    boxShadow: [
      BoxShadow(
        color: Color(0x0D000000),
        blurRadius: 15,
        offset: Offset(0, 6),
      )
    ],
  )
  child: Column(...)
Layout context:
  Parent: Column (Main) - 1st child
// WHY: Creates a distinct, elevated card for the primary "Choose your trip" action.

WIDGET INSTANCE:
Type: ListTile (Vehicle Header)
Constructor arguments:
  leading: Container(
    width: 50,
    height: 50,
    decoration: BoxDecoration(
      color: Color(0xFFF5F5F5),
      borderRadius: BorderRadius.circular(12),
    ),
    child: Icon(Icons.local_shipping, size: 32, color: AppTheme.brandNavy),
  )
  title: Text(
    "Choose your trip",
    style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold, color: Colors.black),
  )
  subtitle: Text(
    "Select your preferred delivery vehicle.",
    style: TextStyle(fontSize: 13, color: Colors.black),
  )
  trailing: AnimatedRotation(
    turns: isExpanded ? 0.5 : 0,
    duration: const Duration(milliseconds: 300),
    child: const Icon(Icons.keyboard_arrow_down, color: Colors.black),
  )
  onTap: () {
    setState(() {
      isExpanded = !isExpanded;
    });
  }
Layout context:
  Parent: Column (Vehicle Section)
// WHY: Interactive header that toggles the visibility of vehicle options. AnimatedRotation provides visual feedback for the expansion state.

WIDGET INSTANCE (Inside Vehicle Section - Conditional):
Condition: if (isExpanded)
Type: Padding
Constructor arguments:
  padding: const EdgeInsets.all(10.0)
  child: Column(
    children: [
      _buildVehicleBox(image: 'assets/images/truck_send.png', label: 'TruckSend'),
      _buildVehicleBox(image: 'assets/images/go_fast.png', label: 'GoFast'),
      _buildVehicleBox(image: 'assets/images/frozen_go.png', label: 'FrozenGo'),
      _buildVehicleBox(image: 'assets/images/heavy_load.png', label: 'HeavyLoad'),
      _buildVehicleBox(image: 'assets/images/easy_go.png', label: 'EasyGo'),
    ],
  )
Layout context:
  Parent: Column (Vehicle Section)
// WHY: Reveals the specific vehicle choices when the user taps the header.

WIDGET INSTANCE (Inside _buildVehicleBox - Expanded):
Type: InkWell
Constructor arguments:
  onTap: () {
    context.push('/customer/post-job');
  }
  child: Container(
    height: 80.0,
    margin: const EdgeInsets.symmetric(vertical: 6),
    padding: const EdgeInsets.symmetric(horizontal: 16),
    decoration: BoxDecoration(
      color: Colors.white,
      borderRadius: BorderRadius.circular(12),
      border: Border.all(color: Colors.black12),
    ),
    child: Row(
      children: [
        Image.asset(
          image, // e.g., 'assets/images/truck_send.png'
          width: 60,
          height: 60,
        ),
        const SizedBox(width: 15),
        // WHY: Provides horizontal spacing between the vehicle image and the text label.
        Expanded(
          child: Text(
            label, // e.g., 'TruckSend'
            style: const TextStyle(
              fontSize: 16,
              fontWeight: FontWeight.bold,
              color: Colors.black,
            ),
          ),
        ),
        const Icon(Icons.chevron_right, color: Colors.black54),
      ],
    ),
  )
Layout context:
  Parent: Column inside Padding
// WHY: A selectable row for a specific vehicle type. Navigates to the job posting flow.

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 20.0
Layout context:
  Parent: Column (Main) - 2nd child
// WHY: Provides vertical separation between the Vehicle Section and the Wallet Card.

WIDGET INSTANCE:
Type: Container (Wallet Card)
Constructor arguments:
  width: double.infinity
  padding: const EdgeInsets.all(20)
  decoration: BoxDecoration(
    borderRadius: BorderRadius.circular(22),
    gradient: const LinearGradient(
      colors: [Color(0xFF18398F), Color(0xFF142A5C)],
      begin: Alignment.topLeft,
      end: Alignment.bottomRight,
    ),
    boxShadow: [
      BoxShadow(
        color: Color(0x2E000000),
        blurRadius: 25,
        offset: Offset(0, 12),
      )
    ],
  )
  child: Column(...)
Layout context:
  Parent: Column (Main) - 3rd child
// WHY: Displays the user's financial status in a high-contrast, premium gradient container.

WIDGET INSTANCE (Inside Wallet Card):
Type: Row (Balance Header)
Constructor arguments:
  mainAxisAlignment: MainAxisAlignment.spaceBetween
  children: [
    Text("Balance", style: TextStyle(color: Colors.white70, fontSize: 16)),
    Icon(Icons.account_balance_wallet, color: Colors.white54, size: 20),
  ]
Layout context:
  Parent: Column (Wallet Card)
// WHY: Labels the primary value of the card.

WIDGET INSTANCE (Inside Wallet Card):
Type: Text (Balance Amount)
Constructor arguments:
  data: "EGP 0.00"
  style: TextStyle(color: Colors.white, fontSize: 32, fontWeight: FontWeight.bold)
Layout context:
  Parent: Column (Wallet Card)
// WHY: The most prominent piece of information in the wallet section.

WIDGET INSTANCE (Inside Wallet Card):
Type: SizedBox
Constructor arguments:
  height: 25.0
Layout context:
  Parent: Column (Wallet Card)
// WHY: Separates the balance amount from the action buttons.

WIDGET INSTANCE (Inside Wallet Card):
Type: Row (Actions Row)
Constructor arguments:
  mainAxisAlignment: MainAxisAlignment.spaceBetween
  children: [
    _walletAction(
      icon: Icons.add_circle_outline,
      label: "Top Up",
      onTap: () {
        Navigator.push(
          context,
          MaterialPageRoute(builder: (context) => const CardScreen()),
        );
      },
    ),
    _walletAction(
      icon: Icons.account_balance_wallet_outlined,
      label: "Wallet",
      onTap: () {
        Navigator.push(
          context,
          MaterialPageRoute(builder: (context) => const WalletScreen()),
        );
      },
    ),
    _walletAction(
      icon: Icons.history,
      label: "History",
      onTap: () {
        Navigator.push(
          context,
          MaterialPageRoute(builder: (context) => const WalletScreen()),
        );
      },
    ),
  ]
Layout context:
  Parent: Column (Wallet Card)
// WHY: Provides quick access to financial operations.

WIDGET INSTANCE (Inside _walletAction - Expanded):
Type: GestureDetector
Constructor arguments:
  onTap: onTap // (Defined in Row children above)
  child: Column(
    children: [
      Container(
        padding: const EdgeInsets.all(10),
        decoration: BoxDecoration(
          color: Colors.white.withOpacity(0.15),
          shape: BoxShape.circle,
        ),
        child: Icon(icon, color: Colors.white, size: 24),
      ),
      const SizedBox(height: 8),
      // WHY: Vertical spacing between the action icon and its text label.
      Text(
        label,
        style: const TextStyle(color: Colors.white, fontSize: 12),
      ),
    ],
  )
Layout context:
  Parent: Row
// WHY: A vertically aligned icon-label pair for wallet actions.

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 20.0
Layout context:
  Parent: Column (Main) - 4th child
// WHY: Provides vertical separation between the Wallet Card and the Logistics Banner.

WIDGET INSTANCE:
Type: Container (Logistics Banner)
Constructor arguments:
  width: double.infinity
  padding: const EdgeInsets.all(20)
  decoration: BoxDecoration(
    borderRadius: BorderRadius.circular(20),
    gradient: const LinearGradient(
      colors: [Color(0xFF0A3EC6), Color(0xFF0EC8EE)],
      begin: Alignment.topLeft,
      end: Alignment.bottomRight,
    ),
    boxShadow: [
      BoxShadow(
        color: Color(0x1F000000),
        blurRadius: 20,
        offset: Offset(0, 10),
      )
    ],
  )
  child: Column(...)
Layout context:
  Parent: Column (Main) - 5th child
// WHY: Captures user attention for the core delivery booking feature using a vibrant blue-cyan gradient.

WIDGET INSTANCE (Inside Logistics Banner):
Type: Text (Banner Title)
Constructor arguments:
  data: "Move Anything, Anywhere"
  style: TextStyle(color: Colors.white, fontSize: 22, fontWeight: FontWeight.bold)
Layout context:
  Parent: Column (Logistics Banner)
// WHY: Clear value proposition for the logistics service.

WIDGET INSTANCE (Inside Logistics Banner):
Type: Text (Banner Subtitle)
Constructor arguments:
  data: "Fast, secure, and reliable delivery service for all your needs."
  style: TextStyle(color: Colors.white.withOpacity(0.9), fontSize: 14)
Layout context:
  Parent: Column (Logistics Banner)
// WHY: Explains the benefits of using MoveIt.

WIDGET INSTANCE (Inside Logistics Banner):
Type: SizedBox
Constructor arguments:
  height: 20.0
Layout context:
  Parent: Column (Logistics Banner)
// WHY: Spacing before the "Book Now" action button.

WIDGET INSTANCE (Inside Logistics Banner):
Type: Align
Constructor arguments:
  alignment: Alignment.bottomRight
  child: ElevatedButton(
    onPressed: () {
      context.push('/customer/post-job');
    },
    style: ElevatedButton.styleFrom(
      backgroundColor: Colors.white,
      foregroundColor: Colors.black,
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14)),
      padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 12),
    ),
    child: const Text(
      "Book Now",
      style: TextStyle(fontWeight: FontWeight.bold),
    ),
  )
Layout context:
  Parent: Column (Logistics Banner)
// WHY: Primary call-to-action button for initiating a delivery request.

CONTINUE TO NEXT FILE

=== FILE: lib/features/customer/customer_jobs_screen.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  body: jobsAsync.when(...)
Layout context:
  Parent: CustomerJobsScreen.build
// WHY: Provides the root layout for the job history screen.

WIDGET INSTANCE (jobsAsync.when - loading):
Type: ListView.builder
Constructor arguments:
  itemCount: 5
  itemBuilder: (context, index) {
    return const JobCardShimmer();
  }
Layout context:
  Parent: Scaffold.body
// WHY: Provides a list of shimmering placeholders to indicate that data is currently being fetched.

WIDGET INSTANCE (Inside JobCardShimmer - Expanded):
Type: Padding
Constructor arguments:
  padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8)
  child: Shimmer.fromColors(
    baseColor: Colors.grey[300]!,
    highlightColor: Colors.grey[100]!,
    child: Container(
      height: 120,
      decoration: BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.circular(16),
      ),
    ),
  ),
Layout context:
  Parent: ListView.builder
// WHY: Visually mimics a job card with a pulsing animation to keep the user engaged during wait times.

WIDGET INSTANCE (jobsAsync.when - error):
Type: ErrorStateWidget
Constructor arguments:
  message: e.toString()
  onRetry: () {
    ref.invalidate(customerJobsProvider);
  }
Layout context:
  Parent: Scaffold.body
// WHY: Displays a friendly error message and provides a way to reload the data if the Firestore request fails.

WIDGET INSTANCE (Inside ErrorStateWidget - Expanded):
Type: Center
Constructor arguments:
  child: Padding(
    padding: const EdgeInsets.all(24.0),
    child: Column(
      mainAxisSize: MainAxisSize.min,
      children: [
        const Icon(
          Icons.error_outline_rounded,
          size: 80,
          color: AppTheme.brandError,
        ),
        const SizedBox(height: 24),
        // WHY: Vertical spacing between the error icon and the title.
        Text(
          'Something went wrong',
          style: Theme.of(context).textTheme.headlineMedium,
          textAlign: TextAlign.center,
        ),
        const SizedBox(height: 12),
        // WHY: Vertical spacing between the title and the specific error message.
        Text(
          message,
          style: Theme.of(context).textTheme.bodyLarge?.copyWith(color: Colors.grey[600]),
          textAlign: TextAlign.center,
        ),
        const SizedBox(height: 32),
        // WHY: Vertical spacing before the retry button.
        ElevatedButton(
          onPressed: onRetry, // (Invalidates provider as defined above)
          child: const Text('Try Again'),
        ),
      ],
    ),
  ),
Layout context:
  Parent: Scaffold.body
// WHY: Full-screen error state implementation.

WIDGET INSTANCE (jobsAsync.when - data - Empty State):
Condition: if (jobs.isEmpty)
Type: Center
Constructor arguments:
  child: Text(
    "You haven't posted any jobs yet.",
    style: TextStyle(color: Colors.grey[600], fontSize: 16),
  )
Layout context:
  Parent: Scaffold.body
// WHY: Informs the user when they have no history to display.

WIDGET INSTANCE (jobsAsync.when - data - List State):
Condition: else
Type: ListView.builder
Constructor arguments:
  itemCount: jobs.length
  itemBuilder: (context, index) {
    return _JobCard(job: jobs[index]);
  }
Layout context:
  Parent: Scaffold.body
// WHY: Renders the list of jobs retrieved from Firestore.

WIDGET INSTANCE (Inside _JobCard - Expanded):
Type: Card
Constructor arguments:
  margin: const EdgeInsets.symmetric(horizontal: 16, vertical: 8)
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16))
  child: ListTile(...)
Layout context:
  Parent: ListView.builder
// WHY: Groups job details into a clean, rounded card.

WIDGET INSTANCE (Inside _JobCard -> ListTile):
Type: ListTile
Constructor arguments:
  contentPadding: const EdgeInsets.all(16)
  title: Text(
    job.itemDescription,
    maxLines: 1,
    overflow: TextOverflow.ellipsis,
    style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 16),
  )
  subtitle: Column(
    crossAxisAlignment: CrossAxisAlignment.start,
    children: [
      const SizedBox(height: 8),
      // WHY: Spacing between the title and the creation date.
      Row(
        children: [
          Icon(Icons.access_time, size: 14, color: Colors.grey[600]),
          const SizedBox(width: 4),
          // WHY: Spacing between the clock icon and the date text.
          Text(
            DateFormat('MMM d, h:mm a').format(job.createdAt),
            style: TextStyle(fontSize: 12, color: Colors.grey[600]),
          ),
        ],
      ),
      const SizedBox(height: 8),
      // WHY: Spacing between the date row and the status chip.
      _StatusChip(status: job.status),
    ],
  )
  trailing: Column(
    mainAxisAlignment: MainAxisAlignment.center,
    crossAxisAlignment: CrossAxisAlignment.end,
    children: [
      Text(
        NumberFormat.currency(symbol: 'EGP ', decimalDigits: 2).format(job.pricePiastres / 100),
        style: const TextStyle(
          fontWeight: FontWeight.bold,
          color: AppTheme.brandOrange,
          fontSize: 16,
        ),
      ),
      const Icon(Icons.chevron_right, color: Colors.grey),
    ],
  )
  onTap: () {
    // Navigates to detail page using jobId
    context.push('/customer/jobs/\');
  }
Layout context:
  Parent: Card
// WHY: Displays key job information (description, date, status, price) in a single row with an invitation to tap for more details.

WIDGET INSTANCE (Inside _StatusChip - Expanded):
Type: Container
Constructor arguments:
  padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 4)
  decoration: BoxDecoration(
    color: (status == 'pending' ? Colors.grey : 
           status == 'accepted' ? AppTheme.brandSkyBlue : 
           status == 'in_transit' ? AppTheme.brandOrange : 
           status == 'delivered' ? AppTheme.brandSuccess : Colors.grey).withOpacity(0.1),
    borderRadius: BorderRadius.circular(12),
    border: Border.all(
      color: (status == 'pending' ? Colors.grey : 
             status == 'accepted' ? AppTheme.brandSkyBlue : 
             status == 'in_transit' ? AppTheme.brandOrange : 
             status == 'delivered' ? AppTheme.brandSuccess : Colors.grey).withOpacity(0.5)
    ),
  )
  child: Row(
    mainAxisSize: MainAxisSize.min,
    children: [
      Icon(
        status == 'pending' ? Icons.timer_outlined : 
        status == 'accepted' ? Icons.check_circle_outline : 
        status == 'in_transit' ? Icons.local_shipping_outlined : 
        status == 'delivered' ? Icons.done_all : Icons.help_outline,
        size: 12,
        color: (status == 'pending' ? Colors.grey : 
               status == 'accepted' ? AppTheme.brandSkyBlue : 
               status == 'in_transit' ? AppTheme.brandOrange : 
               status == 'delivered' ? AppTheme.brandSuccess : Colors.grey),
      ),
      const SizedBox(width: 4),
      // WHY: Spacing between the status icon and the uppercase status label.
      Text(
        status.replaceAll('_', ' ').toUpperCase(),
        style: TextStyle(
          color: (status == 'pending' ? Colors.grey : 
                 status == 'accepted' ? AppTheme.brandSkyBlue : 
                 status == 'in_transit' ? AppTheme.brandOrange : 
                 status == 'delivered' ? AppTheme.brandSuccess : Colors.grey),
          fontSize: 10,
          fontWeight: FontWeight.bold,
        ),
      ),
    ],
  )
Layout context:
  Parent: Column inside _JobCard subtitle
// WHY: Provides a visual shorthand for the job's progress through its lifecycle.

CONTINUE TO NEXT FILE

=== FILE: lib/features/customer/customer_job_detail_screen.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  body: Stack(...)
Layout context:
  Parent: CustomerJobDetailScreen.build
// WHY: Root container for the detail view, allowing the bottom sheet to overlay the full-screen map.

WIDGET INSTANCE:
Type: Stack
Constructor arguments:
  children: [FlutterMap, Positioned (Back Button), DraggableScrollableSheet]
Layout context:
  Parent: Scaffold.body
// WHY: Enables layered UI components, placing the map at the base and the controls on top.

WIDGET INSTANCE:
Type: FlutterMap
Constructor arguments:
  mapController: mapController
  options: const MapOptions(
    initialCenter: LatLng(30.0444, 31.2357),
    initialZoom: 13,
  )
  children: [TileLayer, MarkerLayer (Conditional)]
Layout context:
  Parent: Stack (1st child)
// WHY: Displays the geographic context of the delivery job.

WIDGET INSTANCE:
Type: TileLayer
Constructor arguments:
  urlTemplate: 'https://tile.openstreetmap.org/{z}/{x}/{y}.png'
  userAgentPackageName: 'com.moveit.egypt'
Layout context:
  Parent: FlutterMap.children
// WHY: Provides the actual map imagery from OpenStreetMap.

WIDGET INSTANCE (Inside FlutterMap - Conditional):
Condition: if (job.value != null)
Type: MarkerLayer
Constructor arguments:
  markers: [
    Marker(
      point: LatLng(job.value!.pickupLatLng.latitude, job.value!.pickupLatLng.longitude),
      child: const Icon(Icons.home, color: AppTheme.brandSkyBlue, size: 40),
    ),
    Marker(
      point: LatLng(job.value!.dropoffLatLng.latitude, job.value!.dropoffLatLng.longitude),
      child: const Icon(Icons.flag, color: AppTheme.brandOrange, size: 40),
    ),
    if (tracking.value != null)
      Marker(
        point: LatLng(tracking.value!.driverLatLng.latitude, tracking.value!.driverLatLng.longitude),
        rotate: true,
        child: Transform.rotate(
          angle: tracking.value!.heading * (3.14159 / 180),
          child: const Icon(Icons.local_shipping, color: AppTheme.brandNavy, size: 40),
        ),
      ),
  ]
Layout context:
  Parent: FlutterMap.children
// WHY: Visually locates the pickup, dropoff, and live driver positions on the map.

WIDGET INSTANCE:
Type: Positioned (Back Button)
Constructor arguments:
  top: 50.0
  left: 16.0
  child: CircleAvatar(
    backgroundColor: Colors.white,
    child: IconButton(
      icon: const Icon(Icons.arrow_back, color: AppTheme.brandNavy),
      onPressed: () {
        Navigator.pop(context);
      },
    ),
  )
Layout context:
  Parent: Stack (2nd child)
// WHY: Provides a floating, high-contrast button to exit the detail view.

WIDGET INSTANCE:
Type: DraggableScrollableSheet
Constructor arguments:
  initialChildSize: 0.35
  minChildSize: 0.15
  maxChildSize: 0.6
  builder: (context, scrollController) {
    return Container(
      decoration: const BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.vertical(top: Radius.circular(24)),
        boxShadow: [BoxShadow(blurRadius: 10, color: Colors.black12)],
      ),
      child: ListView(...)
    );
  }
Layout context:
  Parent: Stack (3rd child)
// WHY: Interactive bottom sheet that allows the user to swipe up for job details or down to focus on the map.

WIDGET INSTANCE (Inside DraggableScrollableSheet -> ListView):
Type: ListView
Constructor arguments:
  controller: scrollController
  padding: const EdgeInsets.all(24)
  children: [
    Center(child: Container(width: 40, height: 5, decoration: BoxDecoration(color: Colors.grey[300], borderRadius: BorderRadius.circular(5)))),
    const SizedBox(height: 24),
    // WHY: Spacing between the drag handle and the status timeline.
    Row(
      mainAxisAlignment: MainAxisAlignment.spaceBetween,
      children: [
        _StatusStep(label: 'Order', active: true, done: true),
        _StatusStep(label: 'Pickup', active: true, done: false),
        _StatusStep(label: 'Transit', active: false, done: false),
        _StatusStep(label: 'Done', active: false, done: false),
      ],
    ),
    const SizedBox(height: 32),
    // WHY: Spacing between the timeline and the job description text.
    if (job.value != null) ...[
      Text(job.value!.itemDescription, style: const TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),
      const SizedBox(height: 8),
      // WHY: Spacing between the description and the vehicle type label.
      Text(
        'Vehicle: \',
        style: TextStyle(color: Colors.grey[600], fontWeight: FontWeight.bold),
      ),
      const Divider(height: 40),
      // WHY: Horizontal line to separate job details from driver information.
      Row(
        children: [
          const CircleAvatar(backgroundColor: AppTheme.brandSkyBlue, child: Icon(Icons.person, color: Colors.white)),
          const SizedBox(width: 16),
          // WHY: Spacing between the driver's avatar and their name.
          const Expanded(
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Text('Ahmed Mostafa', style: TextStyle(fontWeight: FontWeight.bold)),
                Text('Sedan Car • ABC 123', style: TextStyle(fontSize: 12)),
              ],
            ),
          ),
          IconButton(
            onPressed: () {
              // Action: Dial driver's phone number
              debugPrint('Calling driver...');
            },
            icon: const Icon(Icons.phone, color: AppTheme.brandSuccess),
          ),
        ],
      ),
    ],
  ]
Layout context:
  Parent: Container
// WHY: Displays all textual and interactive information about the job in a scrollable format.

WIDGET INSTANCE (Inside _StatusStep - Expanded):
Type: Column
Constructor arguments:
  children: [
    Container(
      width: 24,
      height: 24,
      decoration: BoxDecoration(
        shape: BoxShape.circle,
        color: done ? AppTheme.brandSuccess : (active ? AppTheme.brandSkyBlue : Colors.grey[300]),
        border: Border.all(color: Colors.white, width: 4),
      ),
      child: done ? const Icon(Icons.check, size: 12, color: Colors.white) : null,
    ),
    const SizedBox(height: 4),
    // WHY: Vertical spacing between the status circle and the text label.
    Text(
      label,
      style: TextStyle(
        fontSize: 10,
        color: active ? AppTheme.brandNavy : Colors.grey,
        fontWeight: active ? FontWeight.bold : FontWeight.normal,
      ),
    ),
  ]
Layout context:
  Parent: Row inside DraggableScrollableSheet
// WHY: A single point in the job progress timeline.

CONTINUE TO NEXT FILE

=== FILE: lib/features/customer/post_job_screen.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  backgroundColor: Colors.white
  appBar: AppBar(...)
  body: PageView(...)
Layout context:
  Parent: PostJobScreen.build
// WHY: Root layout for the multi-step job creation flow.

WIDGET INSTANCE:
Type: AppBar
Constructor arguments:
  backgroundColor: const Color(0xFF0F1F91)
  foregroundColor: Colors.white
  elevation: 0
  leading: IconButton(
    icon: const Icon(Icons.arrow_back),
    onPressed: () {
      if (currentStep.value > 0) {
        currentStep.value--;
        pageController.previousPage(duration: const Duration(milliseconds: 300), curve: Curves.easeInOut);
      } else {
        context.pop();
      }
    },
    color: Colors.white,
  )
  title: Column(...)
  centerTitle: true
Layout context:
  Parent: Scaffold
// WHY: Provides navigation and progress context for the user.

WIDGET INSTANCE (Inside AppBar title):
Type: Column
Constructor arguments:
  mainAxisSize: MainAxisSize.min
  children: [
    Text('Post a Job — Step \ of 4', style: const TextStyle(fontSize: 14, color: Colors.white)),
    const SizedBox(height: 6),
    // WHY: Spacing between the step text and the progress bar.
    LinearProgressIndicator(
      value: (currentStep.value + 1) / 4,
      backgroundColor: Colors.white24,
      valueColor: const AlwaysStoppedAnimation(Colors.white),
      minHeight: 4,
    ),
  ]
Layout context:
  Parent: AppBar
// WHY: Visually communicates how far the user has progressed in the 4-step process.

WIDGET INSTANCE:
Type: PageView
Constructor arguments:
  controller: pageController
  physics: const NeverScrollableScrollPhysics()
  children: [_ItemDetailsStep, _LocationPickerStep (Pickup), _LocationPickerStep (Dropoff), _ReviewStep]
Layout context:
  Parent: Scaffold.body
// WHY: Manages the transitions between the different steps of the form.

WIDGET INSTANCE (Inside _ItemDetailsStep - Expanded):
Type: Container
Constructor arguments:
  color: Colors.white
  padding: const EdgeInsets.all(24)
  child: Column(
    crossAxisAlignment: CrossAxisAlignment.stretch,
    children: [
      const SizedBox(height: 10),
      // WHY: Top padding to align content below the status bar/app bar area.
      const Text('What are you sending?', style: TextStyle(color: Color(0xFF113465), fontSize: 22, fontWeight: FontWeight.bold)),
      const SizedBox(height: 8),
      // WHY: Spacing between the main question and the helper instruction.
      const Text('Describe the item so drivers know what to expect.', style: TextStyle(color: Color(0xFF112F5A), fontSize: 14)),
      const SizedBox(height: 32),
      // WHY: Spacing before the text input field.
      TextField(
        controller: controller,
        maxLines: 4,
        maxLength: 200,
        style: const TextStyle(color: Colors.black, fontSize: 14),
        decoration: InputDecoration(
          labelText: 'Item description',
          hintText: 'e.g. Medium box of books, fragile',
          labelStyle: const TextStyle(color: Color(0xFF113463)),
          filled: true,
          fillColor: const Color(0xFFF2F2F2),
          border: OutlineInputBorder(borderRadius: BorderRadius.circular(14), borderSide: BorderSide.none),
        ),
      ),
      const Spacer(),
      // WHY: Pushes the "Continue" button to the bottom of the screen.
      SizedBox(
        height: 50,
        child: ElevatedButton(
          style: ElevatedButton.styleFrom(backgroundColor: const Color(0xFF0F1F91), shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),
          onPressed: () {
            if (controller.text.trim().isEmpty) {
              CustomSnackBar.show(context, message: 'Please describe your item.', type: SnackBarType.error);
              return;
            }
            onNext(); // (Moves PageView forward)
          },
          child: const Text('Continue', style: TextStyle(color: Colors.white, fontWeight: FontWeight.w600)),
        ),
      ),
    ],
  )
Layout context:
  Parent: PageView (Step 1)
// WHY: Captures the initial item details from the user.

WIDGET INSTANCE (Inside _LocationPickerStep - Expanded):
Type: Stack
Constructor arguments:
  children: [FlutterMap, Center(Pin Icon), Positioned(Search Bar), Positioned(Confirm Panel)]
Layout context:
  Parent: PageView (Step 2 and 3)
// WHY: Provides an interactive map interface for selecting precise pickup and dropoff locations.

WIDGET INSTANCE (Inside _LocationPickerStep -> FlutterMap):
Type: FlutterMap
Constructor arguments:
  mapController: mapController
  options: MapOptions(
    initialCenter: LatLng(30.0444, 31.2357),
    initialZoom: 14,
    onPositionChanged: (pos, hasGesture) async {
      if (!hasGesture || pos.center == null) return;
      center.value = pos.center!;
      final result = await geocoding.reverse(pos.center!);
      result.when(
        success: (addr) => address.value = addr,
        failure: (err) { /* No action, keep old address */ },
      );
    },
  )
  children: [TileLayer(urlTemplate: 'https://tile.openstreetmap.org/{z}/{x}/{y}.png', userAgentPackageName: 'com.moveit.egypt')]
Layout context:
  Parent: Stack
// WHY: Allows the user to pan the map to center the desired location under the pin.

WIDGET INSTANCE (Inside _LocationPickerStep -> Search Bar):
Type: Positioned
Constructor arguments:
  top: 12, left: 12, right: 12
  child: Column(
    children: [
      Material(
        elevation: 4,
        borderRadius: BorderRadius.circular(12),
        child: TextField(
          controller: searchCtrl,
          decoration: InputDecoration(
            hintText: 'Search address…',
            prefixIcon: const Icon(Icons.search, color: Color(0xFF0F1F91)),
            filled: true,
            fillColor: Colors.white,
            border: OutlineInputBorder(borderRadius: BorderRadius.circular(12), borderSide: BorderSide.none),
          ),
          onChanged: (q) async {
            if (q.length < 3) { searchResults.value = []; return; }
            final result = await geocoding.search(q);
            result.when(success: (list) => searchResults.value = list, failure: (err) => {});
          },
        ),
      ),
      if (searchResults.value.isNotEmpty)
        Material(...) // ListView of results
    ],
  )
Layout context:
  Parent: Stack
// WHY: Allows users to search for specific addresses rather than just panning the map.

WIDGET INSTANCE (Inside _LocationPickerStep -> Confirm Panel):
Type: Positioned
Constructor arguments:
  bottom: 0, left: 0, right: 0
  child: Container(
    padding: const EdgeInsets.fromLTRB(24, 20, 24, 32),
    decoration: const BoxDecoration(color: Colors.white, borderRadius: BorderRadius.vertical(top: Radius.circular(24)), boxShadow: [BoxShadow(blurRadius: 10, color: Colors.black12)]),
    child: Column(
      mainAxisSize: MainAxisSize.min,
      children: [
        Text(title, style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 16, color: Colors.black)),
        const SizedBox(height: 6),
        // WHY: Spacing between the step title and the resolved address text.
        Text(address.value, maxLines: 2, textAlign: TextAlign.center, style: TextStyle(color: Colors.grey[600], fontSize: 12)),
        const SizedBox(height: 20),
        // WHY: Spacing before the confirmation button.
        SizedBox(
          width: double.infinity,
          height: 50,
          child: ElevatedButton(
            style: ElevatedButton.styleFrom(backgroundColor: const Color(0xFF0F1F91), shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),
            onPressed: () {
              onConfirmed(center.value, address.value); // (Saves location and moves to next step)
            },
            child: Text('Confirm \', style: const TextStyle(color: Colors.white, fontWeight: FontWeight.bold)),
          ),
        ),
      ],
    ),
  )
Layout context:
  Parent: Stack
// WHY: Summarizes the selected location and provides the primary action to proceed.

WIDGET INSTANCE (Inside _ReviewStep - Expanded):
Type: Container
Constructor arguments:
  color: Colors.white
  padding: const EdgeInsets.all(24)
  child: Column(
    crossAxisAlignment: CrossAxisAlignment.stretch,
    children: [
      const Text('Review & Post', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold, color: Color(0xFF172057))),
      const SizedBox(height: 24),
      // WHY: Spacing between the title and the review summary box.
      Container(
        padding: const EdgeInsets.all(20),
        decoration: BoxDecoration(color: const Color(0xFF0F1F91), borderRadius: BorderRadius.circular(20)),
        child: Column(
          children: [
            _ReviewRow(label: 'Item', value: description, icon: Icons.inventory_2_outlined),
            const Divider(color: Colors.white24),
            // WHY: Separator between item description and pickup location.
            _ReviewRow(label: 'Pickup', value: pickup, icon: Icons.location_on_outlined),
            const Divider(color: Colors.white24),
            // WHY: Separator between pickup and dropoff locations.
            _ReviewRow(label: 'Dropoff', value: dropoff, icon: Icons.flag_outlined),
          ],
        ),
      ),
      const Spacer(),
      // WHY: Pushes the final "Post Job" button to the bottom of the screen.
      SizedBox(
        height: 52,
        child: ElevatedButton(
          style: ElevatedButton.styleFrom(backgroundColor: const Color(0xFFFF8A00), shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(14))),
          onPressed: () async {
            final authUser = ref.read(authStateChangesProvider).value;
            if (authUser == null) return;
            final job = JobModel(
              id: '', customerId: authUser.uid, status: 'pending',
              pickupLatLng: GeoPoint(pickupLocation.value!.latitude, pickupLocation.value!.longitude),
              dropoffLatLng: GeoPoint(dropoffLocation.value!.latitude, dropoffLocation.value!.longitude),
              pickupAddress: pickupAddress.value, dropoffAddress: dropoffAddress.value,
              itemDescription: descriptionCtrl.text, vehicleTypeRequired: 'motorcycle',
              pricePiastres: 5000, createdAt: DateTime.now(),
            );
            try {
              await createJob(job);
              if (context.mounted) {
                CustomSnackBar.show(context, message: 'Job posted successfully!', type: SnackBarType.success);
                context.go('/customer');
              }
            } catch (e) {
              if (context.mounted) {
                CustomSnackBar.show(context, message: 'Error posting job: \', type: SnackBarType.error);
              }
            }
          },
          child: const Text('Post Job', style: TextStyle(color: Colors.white, fontSize: 16, fontWeight: FontWeight.bold)),
        ),
      ),
    ],
  )
Layout context:
  Parent: PageView (Step 4)
// WHY: Final confirmation step where all captured data is summarized before being sent to Firestore.

WIDGET INSTANCE (Inside _ReviewRow - Expanded):
Type: Padding
Constructor arguments:
  padding: const EdgeInsets.symmetric(vertical: 10)
  child: Row(
    crossAxisAlignment: CrossAxisAlignment.start,
    children: [
      Icon(icon, color: const Color(0xFFFF8A00), size: 22),
      const SizedBox(width: 14),
      // WHY: Horizontal spacing between the category icon and the text labels.
      Expanded(
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Text(label, style: const TextStyle(color: Colors.white70, fontWeight: FontWeight.w600, fontSize: 13)),
            const SizedBox(height: 4),
            // WHY: Spacing between the field label and its value.
            Text(value, style: const TextStyle(color: Colors.white, fontSize: 15, fontWeight: FontWeight.w500)),
          ],
        ),
      ),
    ],
  )
Layout context:
  Parent: Column inside Review Box
// WHY: A clean, labeled data row for summarizing job details.

CONTINUE TO NEXT FILE

=== FILE: lib/features/driver/driver_shell.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  appBar: MoveItAppBar(title: title.isEmpty ? null : Text(title, style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 18)))
  endDrawer: const MoveItDrawer()
  body: child
  bottomNavigationBar: BottomNavigationBar(...)
Layout context:
  Parent: DriverShell.build
// WHY: Provides the shared navigation structure (AppBar, Drawer, BottomNav) for all driver-facing screens.

WIDGET INSTANCE (Inside MoveItAppBar - Expanded):
Type: AppBar
Constructor arguments:
  backgroundColor: AppTheme.brandNavy
  elevation: 4
  title: title == null ? RichText(
    text: const TextSpan(
      children: [
        TextSpan(text: 'M', style: TextStyle(color: Color(0xFF1265B3), fontSize: 26, fontWeight: FontWeight.bold)),
        TextSpan(text: 'ove', style: TextStyle(color: Colors.lightBlueAccent, fontSize: 26, fontWeight: FontWeight.bold)),
        TextSpan(text: 'It', style: TextStyle(color: Colors.yellow, fontSize: 26, fontWeight: FontWeight.bold)),
      ],
    ),
  ) : Text(title, style: const TextStyle(color: Colors.white, fontWeight: FontWeight.bold))
  centerTitle: false
  actions: [
    Builder(
      builder: (context) => IconButton(
        icon: const Icon(Icons.menu, color: Colors.yellow),
        iconSize: 32,
        onPressed: () {
          Scaffold.of(context).openEndDrawer();
        },
      ),
    ),
  ]
Layout context:
  Parent: Scaffold.appBar
// WHY: Unified application header with branding and drawer access.

WIDGET INSTANCE (Inside MoveItDrawer - Expanded):
Type: Drawer
Constructor arguments:
  backgroundColor: const Color(0xFFF8F9FD)
  child: SafeArea(
    child: Column(
      children: [
        Container(
          width: double.infinity,
          padding: const EdgeInsets.symmetric(horizontal: 20, vertical: 24),
          decoration: const BoxDecoration(
            color: Color(0xFF0F1F91),
            borderRadius: BorderRadius.only(bottomLeft: Radius.circular(28), bottomRight: Radius.circular(28)),
          ),
          child: Column(...) // User details
        ),
        const SizedBox(height: 16),
        // WHY: Spacing between the header and the navigation menu list.
        Expanded(
          child: ListView(
            padding: const EdgeInsets.symmetric(horizontal: 14),
            children: [
              _buildTile(icon: Icons.person_outline, title: "My Profile", onTap: () { Navigator.pop(context); context.go('/driver/profile'); }),
              _buildTile(icon: Icons.history, title: "Trip History", onTap: () { Navigator.pop(context); context.go('/driver/history'); }),
              _buildTile(icon: Icons.settings, title: "Settings", onTap: () { Navigator.pop(context); Navigator.push(context, MaterialPageRoute(builder: (context) => const SettingsPage())); }),
            ],
          ),
        ),
      ],
    ),
  )
Layout context:
  Parent: Scaffold.endDrawer
// WHY: Side navigation menu for quick access to profile and settings.

WIDGET INSTANCE:
Type: BottomNavigationBar
Constructor arguments:
  currentIndex: getCurrentIndex() // (Logic mapping route to index 0-3)
  onTap: (index) {
    switch (index) {
      case 0: context.go('/driver'); break;
      case 1: context.go('/driver/active'); break;
      case 2: context.go('/driver/history'); break;
      case 3: context.go('/driver/profile'); break;
    }
  }
  selectedItemColor: AppTheme.brandOrange
  unselectedItemColor: Colors.grey
  items: const [
    BottomNavigationBarItem(icon: Icon(Icons.dashboard_outlined), activeIcon: Icon(Icons.dashboard), label: 'Board'),
    BottomNavigationBarItem(icon: Icon(Icons.local_shipping_outlined), activeIcon: Icon(Icons.local_shipping), label: 'Active'),
    BottomNavigationBarItem(icon: Icon(Icons.history_outlined), activeIcon: Icon(Icons.history), label: 'History'),
    BottomNavigationBarItem(icon: Icon(Icons.person_outline), activeIcon: Icon(Icons.person), label: 'Profile'),
  ]
Layout context:
  Parent: Scaffold.bottomNavigationBar
// WHY: Primary navigation control for switching between core driver functional areas.

CONTINUE TO NEXT FILE

=== FILE: lib/features/driver/job_board_screen.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE (jobsAsync.when - loading):
Type: Scaffold
Constructor arguments:
  body: Center(
    child: CircularProgressIndicator(
      valueColor: AlwaysStoppedAnimation<Color>(AppTheme.brandNavy),
    ),
  )
Layout context:
  Parent: JobBoardScreen.build
// WHY: Shows a loading indicator while the list of pending jobs is being streamed from Firestore.

WIDGET INSTANCE (jobsAsync.when - data):
Type: Scaffold
Constructor arguments:
  body: Stack(children: [FlutterMap, DraggableScrollableSheet])
Layout context:
  Parent: JobBoardScreen.build
// WHY: Main dashboard for drivers to find and view available jobs.

WIDGET INSTANCE (Inside Stack -> FlutterMap):
Type: FlutterMap
Constructor arguments:
  mapController: mapController
  options: const MapOptions(initialCenter: LatLng(30.0444, 31.2357), initialZoom: 12)
  children: [
    TileLayer(urlTemplate: 'https://tile.openstreetmap.org/{z}/{x}/{y}.png', userAgentPackageName: 'com.moveit.egypt'),
    MarkerLayer(
      markers: jobs.map((job) => Marker(
        point: LatLng(job.pickupLatLng.latitude, job.pickupLatLng.longitude),
        child: const Icon(Icons.location_on, color: AppTheme.brandOrange, size: 30),
      )).toList(),
    ),
  ]
Layout context:
  Parent: Stack
// WHY: Provides a geographic overview of all available jobs, using orange markers for visibility.

WIDGET INSTANCE (Inside Stack -> DraggableScrollableSheet):
Type: DraggableScrollableSheet
Constructor arguments:
  initialChildSize: 0.3
  minChildSize: 0.1
  maxChildSize: 0.8
  builder: (context, scrollController) {
    return Container(
      decoration: const BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.vertical(top: Radius.circular(24)),
        boxShadow: [BoxShadow(blurRadius: 10, color: Colors.black12)],
      ),
      child: jobs.isEmpty 
        ? Center(child: Text('No jobs available for your vehicle type.', style: TextStyle(color: Colors.grey[600])))
        : ListView.builder(
            controller: scrollController,
            padding: const EdgeInsets.all(24),
            itemCount: jobs.length + 1,
            itemBuilder: (context, index) {
              if (index == 0) {
                return Column(
                  children: [
                    Container(width: 40, height: 5, decoration: BoxDecoration(color: Colors.grey[300], borderRadius: BorderRadius.circular(5))),
                    const SizedBox(height: 24),
                    // WHY: Spacing between the drag handle and the "Available Jobs" header.
                    Text('Available Jobs', style: Theme.of(context).textTheme.headlineMedium?.copyWith(fontWeight: FontWeight.bold)),
                    const SizedBox(height: 16),
                    // WHY: Spacing between the header and the first job card.
                  ],
                );
              }
              return _JobBoardCard(job: jobs[index - 1]);
            },
          ),
    );
  }
Layout context:
  Parent: Stack
// WHY: Allows the driver to browse available jobs in a list while still maintaining access to the map.

WIDGET INSTANCE (Inside _JobBoardCard - Expanded):
Type: Card
Constructor arguments:
  margin: const EdgeInsets.only(bottom: 16)
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16))
  child: Padding(
    padding: const EdgeInsets.all(16),
    child: Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Row(
          mainAxisAlignment: MainAxisAlignment.spaceBetween,
          children: [
            Text(job.itemDescription, style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 16, color: Colors.black)),
            Text(
              "\ EGP",
              style: const TextStyle(color: AppTheme.brandOrange, fontWeight: FontWeight.bold, fontSize: 18),
            ),
          ],
        ),
        const SizedBox(height: 8),
        // WHY: Spacing between the item description and the pickup address.
        Row(
          children: [
            const Icon(Icons.location_on_outlined, size: 16, color: Colors.grey),
            const SizedBox(width: 4),
            // WHY: Spacing between the location icon and the address text.
            Expanded(
              child: Text(
                job.pickupAddress,
                style: const TextStyle(color: Colors.grey, fontSize: 13),
                maxLines: 1,
                overflow: TextOverflow.ellipsis,
              ),
            ),
          ],
        ),
        const SizedBox(height: 12),
        // WHY: Spacing before the "View Details" button.
        SizedBox(
          width: double.infinity,
          child: ElevatedButton(
            onPressed: () {
              // Action: Accept job or view full details
              debugPrint('Viewing job: \');
            },
            style: ElevatedButton.styleFrom(
              backgroundColor: AppTheme.brandNavy,
              foregroundColor: Colors.white,
              shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
            ),
            child: const Text('View Details', style: TextStyle(fontWeight: FontWeight.bold)),
          ),
        ),
      ],
    ),
  ),
Layout context:
  Parent: ListView.builder
// WHY: Displays a summary of a single available job for the driver.

CONTINUE TO NEXT FILE

=== FILE: lib/features/driver/active_job_screen.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE (job.value == null):
Type: Scaffold
Constructor arguments:
  body: Center(
    child: Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        const Icon(Icons.local_shipping_outlined, size: 80, color: Colors.grey),
        const SizedBox(height: 16),
        // WHY: Vertical spacing between the empty state icon and the title text.
        const Text('No active delivery', style: TextStyle(color: Colors.grey, fontWeight: FontWeight.bold, fontSize: 18)),
        const SizedBox(height: 8),
        // WHY: Vertical spacing between the title and the descriptive helper text.
        const Text('Accept a job from the board to start.', style: TextStyle(color: Colors.grey, fontSize: 14)),
      ],
    ),
  ),
Layout context:
  Parent: ActiveJobScreen.build
// WHY: Provides clear feedback to the driver when they have no current assignment.

WIDGET INSTANCE (job.value != null):
Type: Scaffold
Constructor arguments:
  body: Stack(children: [FlutterMap, Positioned(Control Panel)])
Layout context:
  Parent: ActiveJobScreen.build
// WHY: Main active tracking view for the driver during a delivery.

WIDGET INSTANCE (Inside Stack -> FlutterMap):
Type: FlutterMap
Constructor arguments:
  mapController: mapController
  options: const MapOptions(initialCenter: LatLng(30.0444, 31.2357), initialZoom: 14)
  children: [
    TileLayer(urlTemplate: 'https://tile.openstreetmap.org/{z}/{x}/{y}.png', userAgentPackageName: 'com.moveit.egypt'),
    MarkerLayer(
      markers: [
        Marker(
          point: LatLng(job.value!.pickupLatLng.latitude, job.value!.pickupLatLng.longitude),
          child: const Icon(Icons.location_on, color: AppTheme.brandSkyBlue, size: 40),
        ),
        Marker(
          point: LatLng(job.value!.dropoffLatLng.latitude, job.value!.dropoffLatLng.longitude),
          child: const Icon(Icons.flag, color: AppTheme.brandOrange, size: 40),
        ),
      ],
    ),
  ]
Layout context:
  Parent: Stack
// WHY: Visualizes the delivery path for the driver.

WIDGET INSTANCE (Inside Stack -> Positioned -> Control Panel):
Type: Container
Constructor arguments:
  padding: const EdgeInsets.all(24)
  decoration: const BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.vertical(top: Radius.circular(24)),
    boxShadow: [BoxShadow(blurRadius: 10, color: Colors.black12)],
  ),
  child: Column(
    mainAxisSize: MainAxisSize.min,
    crossAxisAlignment: CrossAxisAlignment.start,
    children: [
      Row(
        children: [
          _StatusBadge(status: job.value!.status),
          const Spacer(),
          // WHY: Pushes the price text to the right end of the row.
          Text(
            '\ EGP',
            style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 18, color: AppTheme.brandOrange),
          ),
        ],
      ),
      const SizedBox(height: 16),
      // WHY: Spacing between the status/price row and the job description.
      Text(job.value!.itemDescription, style: const TextStyle(fontSize: 20, fontWeight: FontWeight.bold, color: Colors.black)),
      const SizedBox(height: 8),
      // WHY: Spacing between the description and the customer information.
      Row(
        children: [
          const Icon(Icons.person_outline, size: 16, color: Colors.grey),
          const SizedBox(width: 8),
          // WHY: Spacing between the person icon and the customer name text.
          const Text('Customer: Ahmed M.', style: TextStyle(color: Colors.black87, fontSize: 14)),
        ],
      ),
      const Divider(height: 32),
      // WHY: Visual separator between information and action buttons.
      
      if (job.value!.status == 'accepted')
        SizedBox(
          width: double.infinity,
          height: 50,
          child: ElevatedButton(
            onPressed: () {
              // Action: Trigger status update to 'arrived_at_pickup'
              debugPrint('Marking as arrived...');
            },
            style: ElevatedButton.styleFrom(backgroundColor: AppTheme.brandSkyBlue, shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),
            child: const Text("I've Arrived at Pickup", style: TextStyle(color: Colors.white, fontWeight: FontWeight.bold)),
          ),
        )
      else if (job.value!.status == 'in_transit')
        SizedBox(
          width: double.infinity,
          height: 50,
          child: ElevatedButton(
            onPressed: () {
              // Action: Trigger status update to 'delivered'
              debugPrint('Marking as delivered...');
            },
            style: ElevatedButton.styleFrom(backgroundColor: AppTheme.brandSuccess, shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),
            child: const Text("Mark as Delivered", style: TextStyle(color: Colors.white, fontWeight: FontWeight.bold)),
          ),
        ),
    ],
  ),
Layout context:
  Parent: Positioned
// WHY: Provides the primary controls and information for the driver during the active job.

WIDGET INSTANCE (Inside _StatusBadge - Expanded):
Type: Container
Constructor arguments:
  padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 4)
  decoration: BoxDecoration(
    color: AppTheme.brandSkyBlue.withOpacity(0.1),
    borderRadius: BorderRadius.circular(12),
  ),
  child: Text(
    status.toUpperCase(),
    style: const TextStyle(color: AppTheme.brandSkyBlue, fontWeight: FontWeight.bold, fontSize: 12),
  ),
Layout context:
  Parent: Row inside Control Panel
// WHY: A small, high-visibility tag indicating the current state of the delivery.

CONTINUE TO NEXT FILE

=== FILE: lib/features/shared/profile_screen.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  backgroundColor: const Color(0xFFF8FAFF)
  appBar: AppBar(
    backgroundColor: AppTheme.brandNavy,
    title: const Text('My Profile', style: TextStyle(color: Colors.white, fontWeight: FontWeight.bold)),
    centerTitle: true,
    elevation: 0,
    actions: [
      IconButton(
        icon: const Icon(Icons.edit_outlined, color: Colors.white),
        onPressed: () {
          if (user != null) {
            Navigator.push(context, MaterialPageRoute(builder: (context) => EditProfilePage(user: user!)));
          }
        },
      ),
    ],
  )
  body: user == null ? const Center(child: CircularProgressIndicator()) : SingleChildScrollView(...)
Layout context:
  Parent: ProfileScreen.build
// WHY: Main profile view with an option to edit user information.

WIDGET INSTANCE (Inside SingleChildScrollView -> Column):
Type: Column
Constructor arguments:
  children: [Container (Header Background), Transform.translate (Avatar), SizedBox, Text (Name), Text (Email), Padding (Role Chip), Padding (Stats/Details Section), Padding (Logout Button)]
Layout context:
  Parent: SingleChildScrollView
// WHY: Arranges profile elements vertically with a professional, multi-layered look.

WIDGET INSTANCE (Inside Header Background):
Type: Container
Constructor arguments:
  height: 120,
  width: double.infinity,
  decoration: const BoxDecoration(
    color: AppTheme.brandNavy,
    borderRadius: BorderRadius.only(bottomLeft: Radius.circular(40), bottomRight: Radius.circular(40)),
  )
Layout context:
  Parent: Column (1st child)
// WHY: Provides a brand-colored backdrop for the profile avatar.

WIDGET INSTANCE (Inside Stats Section):
Type: Container
Constructor arguments:
  padding: const EdgeInsets.all(24),
  decoration: BoxDecoration(color: Colors.white, borderRadius: BorderRadius.circular(24), boxShadow: [BoxShadow(color: Colors.black.withOpacity(0.05), blurRadius: 20, offset: const Offset(0, 10))]),
  child: Column(
    children: [
      _infoRow(icon: Icons.person_outline, label: 'Full Name', value: user.displayName),
      const Divider(height: 32),
      // WHY: Separates the name field from the email field.
      _infoRow(icon: Icons.email_outlined, label: 'Email Address', value: user.email),
      if (user.isDriver) ...[
        const Divider(height: 32),
        // WHY: Separates the email field from the vehicle information (drivers only).
        _infoRow(icon: Icons.local_shipping_outlined, label: 'Vehicle Type', value: user.vehicleType?.toUpperCase() ?? 'NONE'),
        const Divider(height: 32),
        // WHY: Separates vehicle info from earnings (drivers only).
        _infoRow(icon: Icons.account_balance_wallet_outlined, label: 'Total Earnings', value: '\ EGP', color: AppTheme.brandOrange),
      ],
    ],
  )
Layout context:
  Parent: Padding
// WHY: Displays detailed user attributes in a clean, elevated white card.

WIDGET INSTANCE (Inside _infoRow - Expanded):
Type: Row
Constructor arguments:
  children: [
    Icon(icon, color: AppTheme.brandNavy, size: 22),
    const SizedBox(width: 16),
    // WHY: Spacing between the attribute icon and the text information.
    Expanded(
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(label, style: TextStyle(color: Colors.grey[600], fontSize: 12, fontWeight: FontWeight.w500)),
          const SizedBox(height: 4),
          // WHY: Spacing between the field label and the actual value.
          Text(value, style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold, color: color ?? Colors.black87)),
        ],
      ),
    ),
  ]
Layout context:
  Parent: Column inside Stats Section
// WHY: A standardized row for displaying a single piece of profile information.

WIDGET INSTANCE (Inside Logout Button):
Type: SizedBox(
  width: double.infinity,
  height: 56,
  child: OutlinedButton.icon(
    onPressed: () async {
      final confirm = await showDialog<bool>(
        context: context,
        builder: (context) => AlertDialog(
          title: const Text('Logout'),
          content: const Text('Are you sure you want to sign out?'),
          actions: [
            TextButton(onPressed: () => Navigator.pop(context, false), child: const Text('Cancel')),
            TextButton(onPressed: () => Navigator.pop(context, true), child: const Text('Logout', style: TextStyle(color: Colors.red))),
          ],
        ),
      );
      if (confirm == true) {
        await ref.read(authServiceProvider).signOut();
        if (context.mounted) context.go('/auth');
      }
    },
    icon: const Icon(Icons.logout, color: Colors.red),
    label: const Text('Logout', style: TextStyle(color: Colors.red, fontWeight: FontWeight.bold)),
    style: OutlinedButton.styleFrom(
      side: const BorderSide(color: Colors.red),
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)),
    ),
  ),
)
Layout context:
  Parent: Padding (Last child of Main Column)
// WHY: Provides a prominent and clear action to end the current user session.

CONTINUE TO NEXT FILE

=== FILE: lib/features/auth/login_screen.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  backgroundColor: Colors.white
  body: SingleChildScrollView(...)
Layout context:
  Parent: LoginScreen.build
// WHY: Root container for the authentication screen.

WIDGET INSTANCE (Inside SingleChildScrollView -> Column):
Type: Column
Constructor arguments:
  crossAxisAlignment: CrossAxisAlignment.stretch,
  children: [SizedBox(Header Area), Padding(Form Section)]
Layout context:
  Parent: SingleChildScrollView
// WHY: Arranges the branding and form elements vertically.

WIDGET INSTANCE (Inside Header Area):
Type: Container
Constructor arguments:
  height: 300,
  decoration: const BoxDecoration(
    color: AppTheme.brandNavy,
    borderRadius: BorderRadius.only(bottomRight: Radius.circular(100)),
  )
  child: Center(
    child: Column(
      mainAxisSize: MainAxisSize.min,
      children: [
        const Icon(Icons.local_shipping, color: AppTheme.brandOrange, size: 80),
        const SizedBox(height: 16),
        // WHY: Spacing between the large shipping icon and the application title.
        const Text('MoveIt', style: TextStyle(color: Colors.white, fontSize: 36, fontWeight: FontWeight.bold, letterSpacing: 1.5)),
        const Text('EASY LOGISTICS', style: TextStyle(color: Colors.white70, fontSize: 12, letterSpacing: 4)),
      ],
    ),
  )
Layout context:
  Parent: Column (1st child)
// WHY: Establishes strong visual branding at the top of the login screen.

WIDGET INSTANCE (Inside Form Section):
Type: Column
Constructor arguments:
  crossAxisAlignment: CrossAxisAlignment.stretch,
  children: [
    Text('Welcome Back', style: TextStyle(fontSize: 28, fontWeight: FontWeight.bold, color: AppTheme.brandNavy)),
    const SizedBox(height: 8),
    // WHY: Spacing between the welcome message and the instructions.
    Text('Login to continue your journey', style: TextStyle(color: Colors.grey[600], fontSize: 16)),
    const SizedBox(height: 40),
    // WHY: Significant spacing before the input fields to clear the visual area.
    TextField(
      controller: emailCtrl,
      decoration: InputDecoration(
        labelText: 'Email Address',
        prefixIcon: const Icon(Icons.email_outlined),
        border: OutlineInputBorder(borderRadius: BorderRadius.circular(16)),
      ),
      keyboardType: TextInputType.emailAddress,
    ),
    const SizedBox(height: 20),
    // WHY: Spacing between the email and password fields.
    TextField(
      controller: passCtrl,
      decoration: InputDecoration(
        labelText: 'Password',
        prefixIcon: const Icon(Icons.lock_outline),
        border: OutlineInputBorder(borderRadius: BorderRadius.circular(16)),
      ),
      obscureText: true,
    ),
    const SizedBox(height: 40),
    // WHY: Spacing before the main login button.
    SizedBox(
      height: 56,
      child: ElevatedButton(
        onPressed: isLoading.value ? null : () async {
          if (emailCtrl.text.isEmpty || passCtrl.text.isEmpty) {
            CustomSnackBar.show(context, message: 'Please fill all fields', type: SnackBarType.error);
            return;
          }
          isLoading.value = true;
          final result = await ref.read(authServiceProvider).signIn(email: emailCtrl.text, password: passCtrl.text);
          isLoading.value = false;
          result.when(
            success: (_) => context.go('/auth'),
            failure: (err) => CustomSnackBar.show(context, message: AppError.mapMessage(err), type: SnackBarType.error),
          );
        },
        style: ElevatedButton.styleFrom(backgroundColor: AppTheme.brandNavy, shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16))),
        child: isLoading.value 
          ? const CircularProgressIndicator(color: Colors.white)
          : const Text('LOGIN', style: TextStyle(color: Colors.white, fontSize: 16, fontWeight: FontWeight.bold)),
      ),
    ),
    const SizedBox(height: 24),
    // WHY: Spacing before the registration option.
    TextButton(
      onPressed: () => context.push('/signup'),
      child: RichText(
        text: TextSpan(
          style: TextStyle(color: Colors.grey[600]),
          children: const [
            TextSpan(text: "Don't have an account? "),
            TextSpan(text: "Sign Up", style: TextStyle(color: AppTheme.brandNavy, fontWeight: FontWeight.bold)),
          ],
        ),
      ),
    ),
  ]
Layout context:
  Parent: Padding
// WHY: Captures user credentials and provides feedback for validation or server errors.

CONTINUE TO NEXT FILE

=== FILE: lib/features/auth/signup_screen.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Scaffold
Constructor arguments:
  backgroundColor: Colors.white
  body: SafeArea(child: Center(child: SingleChildScrollView(...)))
Layout context:
  Parent: SignupScreen.build
// WHY: Root layout for the user registration screen, ensuring content stays within device safe areas and is scrollable.

WIDGET INSTANCE (Inside SingleChildScrollView -> Form):
Type: Form
Constructor arguments:
  key: formKey
  child: Column(children: [Image.asset, SizedBox, Text (Sign Up), SizedBox, _RoleToggle, SizedBox, TextFormField (Name), SizedBox, TextFormField (Email), SizedBox, TextFormField (Password), SizedBox, TextFormField (Confirm), AnimatedSize, SizedBox, SizedBox (Submit Button), SizedBox, TextButton (Login Link)])
Layout context:
  Parent: SingleChildScrollView
// WHY: Manages the validation state and arrangement of the signup fields.

WIDGET INSTANCE:
Type: Image.asset
Constructor arguments:
  name: "assets/moveit_master/moveit.png"
  height: 100.0
Layout context:
  Parent: Column (1st child)
// WHY: Displays the application logo to reinforce brand identity during onboarding.

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  height: 20.0
Layout context:
  Parent: Column (2nd child)
// WHY: Provides vertical spacing below the logo image.

WIDGET INSTANCE (Inside Column -> _RoleToggle - Expanded):
Type: Container
Constructor arguments:
  height: 48.0
  decoration: BoxDecoration(color: Colors.grey[100], borderRadius: BorderRadius.circular(12))
  child: Row(children: [_Tab (Customer), _Tab (Driver)])
Layout context:
  Parent: Column (5th child)
// WHY: A toggle component that allows the user to select whether they are signing up as a Customer or a Driver.

WIDGET INSTANCE (Inside _Tab - Expanded):
Type: Expanded
Constructor arguments:
  child: GestureDetector(
    onTap: () {
      HapticFeedback.lightImpact();
      onChanged(value); // (Updates role.value state)
    },
    child: AnimatedContainer(
      duration: const Duration(milliseconds: 200),
      alignment: Alignment.center,
      decoration: BoxDecoration(
        color: active ? AppTheme.brandSkyBlue : Colors.transparent,
        borderRadius: BorderRadius.circular(12),
      ),
      child: Text(
        label, // e.g., 'Customer'
        style: TextStyle(color: active ? Colors.white : AppTheme.brandNavy, fontWeight: FontWeight.bold),
      ),
    ),
  )
Layout context:
  Parent: Row
// WHY: A single selectable tab within the role toggle. Uses animation to transition between selected states.

WIDGET INSTANCE (Inside Column -> AnimatedSize - Conditional):
Condition: if (role.value == 'driver')
Type: Column
Constructor arguments:
  children: [
    const SizedBox(height: 16),
    // WHY: Spacing before the vehicle type dropdown.
    DropdownButtonFormField<String>(
      value: vehicleType.value,
      decoration: InputDecoration(
        labelText: 'Vehicle Type',
        border: OutlineInputBorder(borderRadius: BorderRadius.circular(10)),
      ),
      items: const [
        DropdownMenuItem(value: 'motorcycle', child: Text('???  Motorcycle')),
        DropdownMenuItem(value: 'sedan', child: Text('??  Sedan Car')),
        DropdownMenuItem(value: 'pickup', child: Text('??  Pickup Truck')),
        DropdownMenuItem(value: 'van', child: Text('??  Van')),
        DropdownMenuItem(value: 'truck', child: Text('??  Large Truck')),
      ],
      onChanged: (v) {
        vehicleType.value = v!;
      },
    ),
  ]
Layout context:
  Parent: AnimatedSize
// WHY: Captures vehicle information only when the user selects the 'Driver' role.

WIDGET INSTANCE (Inside Column -> Submit Button):
Type: SizedBox
Constructor arguments:
  width: double.infinity
  height: 56.0
  child: ElevatedButton(
    onPressed: loading.value ? null : () async {
      if (!formKey.currentState!.validate()) {
        HapticFeedback.mediumImpact();
        return;
      }
      loading.value = true;
      final result = await ref.read(authServiceProvider).signUp(
            email: emailCtrl.text.trim(),
            password: passCtrl.text.trim(),
            displayName: nameCtrl.text.trim(),
            role: role.value,
            vehicleType: role.value == 'driver' ? vehicleType.value : null,
          );
      loading.value = false;
      if (!context.mounted) return;
      result.when(
        success: (_) => context.go('/auth'),
        failure: (error) {
          HapticFeedback.heavyImpact();
          CustomSnackBar.show(context, message: AppError.mapMessage(error), type: SnackBarType.error);
        },
      );
    },
    style: ElevatedButton.styleFrom(
      backgroundColor: AppTheme.brandNavy,
      foregroundColor: Colors.white,
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(10)),
    ),
    child: loading.value 
      ? const SizedBox(height: 24, width: 24, child: CircularProgressIndicator(color: Colors.white, strokeWidth: 2))
      : const Text("CONTINUE", style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold)),
  )
Layout context:
  Parent: Column (15th child)
// WHY: Primary action button to execute the signup process. Includes validation, loading state, and error handling with haptic feedback.

CONTINUE TO NEXT FILE

=== FILE: lib/core/widgets/error_state_widget.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Center
Constructor arguments:
  child: Padding(
    padding: const EdgeInsets.all(24.0),
    child: Column(...)
  )
Layout context:
  Parent: ErrorStateWidget.build
// WHY: Centers the error content both vertically and horizontally on the screen.

WIDGET INSTANCE:
Type: Column
Constructor arguments:
  mainAxisSize: MainAxisSize.min
  children: [
    if (lottieAsset != null)
      Lottie.asset(lottieAsset!, height: 200, repeat: true) // Example: 'assets/lottie/error.json'
    else
      const Icon(Icons.error_outline_rounded, size: 80, color: AppTheme.brandError),
    const SizedBox(height: 24),
    // WHY: Provides vertical spacing below the error visual (Lottie or Icon).
    Text(title, style: Theme.of(context).textTheme.headlineMedium, textAlign: TextAlign.center),
    const SizedBox(height: 12),
    // WHY: Provides vertical spacing between the error title and the descriptive message.
    Text(message, style: Theme.of(context).textTheme.bodyLarge?.copyWith(color: Colors.grey[600]), textAlign: TextAlign.center),
    const SizedBox(height: 32),
    // WHY: Provides vertical spacing before the "Try Again" button.
    ElevatedButton(
      onPressed: onRetry, // (Full callback definition is handled in the parent widget calling this, e.g., provider invalidation)
      child: const Text('Try Again'),
    ),
  ]
Layout context:
  Parent: Padding
// WHY: Vertically stacks the error animation/icon, text messages, and recovery action button.

CONTINUE TO NEXT FILE

=== FILE: lib/core/widgets/moveit_drawer.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: Drawer
Constructor arguments:
  backgroundColor: const Color(0xFFF8F9FD)
  child: SafeArea(
    child: Column(...)
  )
Layout context:
  Parent: MoveItDrawer.build
// WHY: Provides the root container for the side navigation menu.

WIDGET INSTANCE (Inside Drawer -> Header):
Type: Container
Constructor arguments:
  width: double.infinity
  padding: const EdgeInsets.symmetric(horizontal: 20, vertical: 24)
  decoration: const BoxDecoration(
    color: Color(0xFF0F1F91),
    borderRadius: BorderRadius.only(bottomLeft: Radius.circular(28), bottomRight: Radius.circular(28)),
  )
  child: Column(
    children: [
      CircleAvatar(
        radius: 38,
        backgroundColor: Colors.white.withOpacity(0.2),
        backgroundImage: user?.photoUrl != null ? NetworkImage(user!.photoUrl!) : null,
        child: user?.photoUrl == null ? const Icon(Icons.person, size: 38, color: Colors.white) : null,
      ),
      const SizedBox(height: 14),
      // WHY: Vertical spacing between the user's avatar and their display name.
      Text(user?.displayName ?? "MoveIt User", style: const TextStyle(color: Colors.white, fontWeight: FontWeight.bold, fontSize: 20)),
      const SizedBox(height: 6),
      // WHY: Vertical spacing between the display name and the email address.
      Text(user?.email ?? "", style: const TextStyle(color: Colors.white70, fontSize: 13)),
      const SizedBox(height: 14),
      // WHY: Vertical spacing before the role tag container.
      Container(
        padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 6),
        decoration: BoxDecoration(color: const Color(0xFFFF8C42), borderRadius: BorderRadius.circular(30)),
        child: Text(user?.role.toUpperCase() ?? "CUSTOMER", style: const TextStyle(color: Colors.white, fontWeight: FontWeight.bold, fontSize: 11)),
      ),
    ],
  )
Layout context:
  Parent: Column (1st child)
// WHY: Displays user identity and role information in a prominent, brand-colored header.

WIDGET INSTANCE (Inside _buildTile - Expanded):
Type: Container
Constructor arguments:
  margin: const EdgeInsets.only(bottom: 12)
  decoration: BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.circular(16),
    boxShadow: [BoxShadow(color: Colors.black.withOpacity(0.04), blurRadius: 10)],
  ),
  child: ListTile(
    leading: Icon(icon, color: iconColor ?? const Color(0xFF0F1F91)),
    title: Text(title, style: TextStyle(fontWeight: FontWeight.w600, color: titleColor ?? Colors.black87)),
    trailing: trailing ?? const Icon(Icons.arrow_forward_ios_rounded, size: 16, color: Colors.grey),
    onTap: onTap // (Function body defined in the caller, e.g., context.go('/profile'))
  ),
Layout context:
  Parent: ListView
// WHY: A standardized menu item widget that provides a clean, card-like interface for navigation.

CONTINUE TO NEXT FILE

=== FILE: lib/core/widgets/moveit_app_bar.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: AppBar
Constructor arguments:
  backgroundColor: AppTheme.brandNavy
  elevation: 4
  title: title ?? RichText(
    text: const TextSpan(
      children: [
        TextSpan(text: 'M', style: TextStyle(color: Color(0xFF1265B3), fontSize: 26, fontWeight: FontWeight.bold)),
        TextSpan(text: 'ove', style: TextStyle(color: Colors.lightBlueAccent, fontSize: 26, fontWeight: FontWeight.bold)),
        TextSpan(text: 'It', style: TextStyle(color: Colors.yellow, fontSize: 26, fontWeight: FontWeight.bold)),
      ],
    ),
  )
  centerTitle: false
  actions: [
    if (showMenu)
      Builder(
        builder: (context) => IconButton(
          icon: const Icon(Icons.menu, color: Colors.yellow),
          iconSize: 32,
          onPressed: () {
            Scaffold.of(context).openEndDrawer();
          },
        ),
      ),
  ]
Layout context:
  Parent: MoveItAppBar.build
// WHY: Centralized application header. The multi-colored RichText logo maintains brand identity, while the conditional menu action ensures navigation access across different contexts.

WIDGET INSTANCE:
Type: SizedBox
Constructor arguments:
  width: 10.0 // (Implicit in Row children or Icon spacing in some contexts)
Layout context:
  N/A (Logic check)
// WHY: Provides horizontal spacing between elements.

=== FILE: lib/core/widgets/custom_snackbar.dart (EXPANDED) ===

#### 3. EXHAUSTIVE WIDGET TREE INVENTORY

WIDGET INSTANCE:
Type: SnackBar
Constructor arguments:
  behavior: SnackBarBehavior.floating
  backgroundColor: AppTheme.brandNavy
  elevation: 6
  margin: const EdgeInsets.all(16)
  padding: EdgeInsets.zero
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))
  content: Container(...)
Layout context:
  Parent: CustomSnackBar.show (Triggered via ScaffoldMessenger)
// WHY: Provides a non-intrusive, floating feedback notification to the user.

WIDGET INSTANCE (Inside SnackBar -> Container):
Type: Container
Constructor arguments:
  height: 60,
  decoration: BoxDecoration(border: Border(left: BorderSide(color: color, width: 6))),
  padding: const EdgeInsets.symmetric(horizontal: 16),
  child: Row(
    children: [
      Icon(icon, color: color),
      const SizedBox(width: 12),
      // WHY: Horizontal spacing between the status icon and the message text.
      Expanded(
        child: Text(message, style: const TextStyle(color: Colors.white, fontSize: 14)),
      ),
    ],
  ),
Layout context:
  Parent: SnackBar.content
// WHY: Wraps the notification content in a styled box with a left-side color accent that indicates the message type (Success, Error, Info).

=== END OF DETAILED EXPANSION UPDATES ===
