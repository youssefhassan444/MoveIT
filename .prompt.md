# Driver UI Redesign — Agent Prompt

Purpose
- Produce a precise, actionable plan and code edits to change the Driver UI layout as described by the user: move the map and app drawer into the Active tab, show jobs in the Board tab, and add the non-functional UI elements shown in the screenshots as well-structured placeholder widgets ready for later wiring.

Scope
- Workspace-scoped. Target Flutter app under `lib/`.
- Focus files and folders: `lib/features/driver/` and `lib/features/shared/` plus new widget files under `lib/features/driver/widgets/`.

Inputs (what the agent should consider)
- Screenshots provided by the user (visual reference).
- Existing code: `pendingJobsProvider`, `acceptJob()`, `activeJobProvider`, `JobModel`, `DriverJobCard`, `FlutterMap` usage.
- Existing theme and constants in `lib/core/theme/app_theme.dart` and `lib/core/constants/app_constants.dart`.
- The app uses `flutter_map` and Riverpod providers.

Desired output
- A small set of patch edits and/or new files that:
  1. Move the map (and related MapController / drawer) from the Board screen into the Active tab screen.
  2. Convert the Board screen into a job-list view with filter chips and an earnings/summary header (non-functional placeholders OK).
  3. Add placeholder, well-structured widgets for all UI elements visible in the screenshots so they can be wired later: ETA card, RouteProgress, DriverProfileCard, ActionButtons (Call/Message/Report), Timeline, DeliveryDetailsCard, EarningsHeader, FilterChips, JobCard enhancements.
  4. Wire accept buttons to `acceptJob()` and keep optimistic UI behavior where appropriate (use `optimisticJobsProvider`).
  5. Provide clear TODO comments in each new file describing where to add real functionality later (tracking subscription, phone launcher, chat screen, etc.).

Implementation guidance (how to edit)
- High-level file changes:
  - Update `lib/features/driver/job_board_screen.dart`:
    - Remove the full-screen `FlutterMap` and change body to a job board list.
    - Add `EarningsHeader` and filter chips at the top of the board.
    - Keep `DriverJobCard` but add small icons/labels (distance, weight, vehicle type) and expose `onAccept` callback.
  - Update `lib/features/driver/active_job_screen.dart`:
    - Add `FlutterMap` with markers/easy-to-replace polyline placeholder.
    - Add a drawer/side menu access placed on the Active tab (floating menu button opens the same drawer content previously global).
    - Add bottom sheet that contains `ETA Card`, `RouteProgress`, `DriverProfileCard`, `ActionButtons` and `Timeline`.
  - Add new widget files under `lib/features/driver/widgets/` (skeletons with constructor args and TODO comments):
    - `earnings_header.dart` (shows earnings, distance, time online)
    - `job_board_filters.dart` (chips: All, Nearby, High Pay, Urgent, My Vehicle)
    - `eta_card.dart` (props: `JobModel job, int etaMinutes, double distanceMiles`)
    - `route_progress.dart` (props: `progressFraction, distanceText`)
    - `driver_profile_card.dart` (props: `name, rating, trips, vehicle, plate, photoUrl`)
    - `action_buttons_row.dart` (props: `onCall, onMessage, onReport` placeholders)
    - `delivery_timeline.dart` (props: `List<TimelineStep>` — skeleton)
    - `delivery_details_card.dart` (props: job fields like order id, items, weight, pickup/dropoff)
    - `job_board_item.dart` (if you want to keep `DriverJobCard` untouched, provide `JobBoardItem` composite that wraps it with additional metadata)
  - Add small `lib/features/driver/widgets/_mock_data.dart` only if required for local visual QA (guard behind `kDebugMode` and removable).

Data binding & providers
- Board tab
  - Use `pendingJobsProvider` to load visible jobs (filter client-side by user vehicle type as already implemented).
  - Watch `optimisticJobsProvider` and merge optimistics into the displayed list, dedup by `job.id`.
  - Wire `Accept` to call `acceptJob(job.id, currentUser.uid)` and show a Snackbar for success/failure (agent needn't change `acceptJob` logic beyond using its return signature if changed).

- Active tab
  - Use `activeJobProvider` to get the driver's active job.
  - Map will use `MapController` and listen to `tracking/{jobId}` when available. Provide placeholder method `subscribeToTracking(jobId)` and TODO comment.
  - Polyline placeholder: create `List<LatLng> route = [];` with TODO to populate from routing API.

Map details & responsiveness
- Keep `FlutterMap` usage but centralize MapOptions in a small helper if helpful. Use `interactionOptions: InteractionOptions(flags: InteractiveFlag.all)` and `keepAlive: true` (as used earlier) for responsiveness.
- Use `MapController` and expose it to children via constructor or Provider if you plan to animate the camera to pickup/dropoff.

UI/UX considerations (non-functional placeholders should reflect behavior)
- Accept flow: `onAccept` should disable the button while awaiting the transaction; show optimistic feedback (move job to active and remove from board) — put TODO markers where to implement optimistic move.
- ETA card: shows ETA minutes, expected arrival time, small status badge (Pending/In Transit). Should accept `onMarkArrived` and `onMarkDelivered` callbacks.
- Action buttons: `onCall` should call `url_launcher` with `tel:` later, `onMessage` should open chat flow, `onReport` should open a report form.
- Timeline: present steps with timestamps; accept an input list of steps and render a vertical timeline.
- Accessibility: large tappable areas for primary actions (Accept, Call, Message), provide semantic labels.

Files to create / modify (suggested)
- Modify: `lib/features/driver/job_board_screen.dart` (board-only layout)
- Modify: `lib/features/driver/active_job_screen.dart` (map + drawer + bottom sheet)
- Add: `lib/features/driver/widgets/earnings_header.dart`
- Add: `lib/features/driver/widgets/job_board_filters.dart`
- Add: `lib/features/driver/widgets/eta_card.dart`
- Add: `lib/features/driver/widgets/route_progress.dart`
- Add: `lib/features/driver/widgets/driver_profile_card.dart`
- Add: `lib/features/driver/widgets/action_buttons_row.dart`
- Add: `lib/features/driver/widgets/delivery_timeline.dart`
- Add: `lib/features/driver/widgets/delivery_details_card.dart`
- (Optional) Add: `lib/features/driver/widgets/job_board_item.dart`

Coding style & conventions
- Use existing `AppTheme` colors and type scale.
- Keep widget constructors explicit and final fields typed.
- Add `TODO` comments where future logic should be implemented (routing service, tracking subscription, launching phone/chat).

Example agent invocation (what this prompt expects)
- Provide a concise command the agent should accept (for CI or automation):

  - `apply-prompt --target lib/features/driver --mode scaffold` — produce new widget files and modify `job_board_screen.dart` and `active_job_screen.dart` to match layout.

- Minimal human invocation:
  - "Use this prompt to scaffold the Driver UI changes and create placeholder widgets for the Active tab map and Board tab job list."

Ambiguities / clarifying questions (please answer)
1. Visual fidelity: Do you want pixel-perfect reproduction of the screenshots or a semantically similar, responsive approximation using existing theme tokens?
2. Assets: Do you have custom icons, profile avatars, or font assets to include, or should I use placeholders and `NetworkImage`/`AssetImage` stubs?
3. Behavior wiring: should accept button perform optimistic move to Active tab automatically for the mock, or only show a success Snackbar and let the developer wire the UI flow later?
4. Drawer location: when you say "move map and drawer to the active tab", do you want the drawer trigger (hamburger) visible on Active only, or the drawer to be accessible from all tabs but opened from Active? (I will default to hamburger visible in Active only.)
5. Filters: should filter chips be functional (client-side filtering of `pendingJobsProvider`) for this scaffold or only visual placeholders?

Deliverables
- A set of scaffolded widget files (skeletons with props and TODOs) placed under `lib/features/driver/widgets/`.
- Two modified screens: `job_board_screen.dart` (board layout) and `active_job_screen.dart` (map + bottom sheet), with comments marking further wiring points.
- A short implementation checklist to apply manually or automatically.

Next steps after you confirm the clarifying answers
1. I will generate concrete patches that add the skeleton widget files and edit the two screens.
2. Run `flutter analyze` locally to ensure no analyzer errors in the changes.
3. Create a small visual smoke-run example using debug-only mock data if you want quick visual verification.

If any part of this scope is too large, tell me which area to scaffold first (e.g., Board UI only, or Active tab only).